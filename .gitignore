# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib
singbox-launcher
# macOS app bundle
*.app
*.app/

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Dependency directories
vendor/

# Go workspace file
go.work

# Build artifacts
rsrc.syso
*.syso

# IDE
.idea/
.vscode/
.cursor/
.cursor-worktree-backup/
*.swp
*.swo
*~

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# Logs
logs/*.log
*.log

# Temporary files
*.tmp
*.temp
# Atomic-swap staging files for config.json / settings.json atomic writes
# (stage to .swap, then os.Rename). Only exists if a write was interrupted.
*.swap
temp_*.txt
compile_debug.txt

# Build directories
dist/
build/dist/
fyne-cross/
releases/

# Test directory with config files
test/

# Test output directory
temp/

# Config files with sensitive data - DO NOT COMMIT!
bin/config.json
bin/config*.json
bin/*.exe
bin/*.dll
# sing-box binary (downloaded artifact)
bin/sing-box

# Backup files
*.bak
*~
config — копия*.json

# Test coverage
coverage.txt
coverage.html

# Night-work reports — personal notes + per-night logs, not for the repo.
docs/night-reports/
