# Рефакторинг get_free.json под единый формат шаблона

## Проблема

Файл `bin/get_free.json` использует ключ `ParserConfig` на верхнем уровне, что не согласовано с новым форматом `wizard_template.json`, где используется `parser_config`. Также отсутствует секция `selectable_rules` для настройки правил маршрутизации, что не согласовано со структурой `state.json`.

## Цель

Привести `get_free.json` к единому формату с `wizard_template.json` и `state.json`:
- Переименовать ключ `ParserConfig` в `parser_config` для согласованности
- Добавить секцию `selectable_rules` для настройки правил из шаблона
- **Планируется:** Добавить секцию `custom_rules` для пользовательских правил (задача не реализована)
- Обеспечить согласованность структуры между шаблонами и `state.json`

## Требования

### 1. Структура parser_config

- **Текущее состояние**: `{"ParserConfig": {...}}` на верхнем уровне
- **Целевое состояние**: `{"parser_config": {...}}` с тем же содержимым
- **Изменение**: Переименование ключа `ParserConfig` → `parser_config` для согласованности с `wizard_template.json`
- **Содержимое**: Структура внутри остается той же (version, proxies, outbounds, parser)

### 2. Секция selectable_rules

- **Назначение**: Упрощенная секция для настройки правил маршрутизации в контексте "Get free VPN"
- **Формат**: Массив объектов, идентичный структуре `selectable_rule_states` из `state.json`
- **Поля** (единообразно с `state.json`):
  - `label` (обязательное) — название правила для отображения в UI
  - `enabled` (обязательное) — включено ли правило по умолчанию
  - `selected_outbound` (обязательное) — выбранный outbound для этого правила
- **Ограничения**: Использовать ТОЛЬКО поля `label`, `enabled` и `selected_outbound`, никаких других полей

### 3. Правило "Russia-only services"

- **Label**: `"Russia-only services"`
- **Enabled**: `true` (включено по умолчанию)
- **SelectedOutbound**: `"ru VPN 🇷🇺"`
- **Назначение**: Настройка правила для сервисов, доступных только в России, чтобы они использовали российский VPN outbound

### 4. Секция custom_rules (планируется)

**⚠️ Планируемая задача:** Добавление секции `custom_rules` не реализовано в текущей версии.

- **Назначение**: Пользовательские правила маршрутизации, созданные вручную (не из шаблона)
- **Формат**: Массив объектов, идентичный структуре `custom_rules` из `state.json`
- **Поля** (единообразно с `state.json`):
  - `label` (обязательное) — название правила
  - `type` (опциональное) — тип правила для отображения в UI: `"IP Addresses (CIDR)"`, `"Domains/URLs"`, `"Processes"`, `"Custom JSON"`
  - `enabled` (обязательное) — включено ли правило
  - `selected_outbound` (обязательное) — выбранный outbound
  - `description` (опциональное) — описание правила
  - `rule` (опциональное) — JSON-объект правила (domain, domain_suffix, ip_cidr и т.д.)
  - `default_outbound` (опциональное) — outbound по умолчанию
  - `has_outbound` (обязательное) — есть ли поле outbound в правиле
- **Важно**: Эти правила хранят полную структуру, т.к. не привязаны к шаблону
- **Статус**: Задача запланирована, но не реализована в текущей версии

### 5. Согласованность с state.json

- Структура `selectable_rules` в `get_free.json` должна быть идентичной структуре `selectable_rule_states` в `state.json`
- **Планируется:** Структура `custom_rules` в `get_free.json` должна быть идентичной структуре `custom_rules` в `state.json` (задача не реализована)
- Использовать те же поля для обеих секций
- Это обеспечивает единообразие между сохраненными состояниями и настройками по умолчанию из `get_free.json`

## Ограничения

- **Только три поля в selectable_rules**: `label`, `enabled`, `selected_outbound` (как в `state.json`)
- **Не добавлять в selectable_rules**: `description`, `rule`, `rule_set`, `platforms`, `default`, `defaultOutbound` и другие поля из полного формата шаблона
- **Полная структура в custom_rules**: Использовать все поля из `PersistedCustomRule` (`label`, `type`, `enabled`, `selected_outbound`, `description`, `rule`, `default_outbound`, `has_outbound`)
- **Упрощенный формат**: `get_free.json` — это упрощенный шаблон для быстрой настройки, не нужно дублировать всю сложность основного шаблона
- **Единообразие**: Структуры должны совпадать с соответствующими секциями из `state.json` для консистентности

## Влияние на код

### Что нужно обновить

1. **Парсинг get_free.json**:
   - Обновить код, который читает `ParserConfig` → должен читать `parser_config`
   - Обеспечить обратную совместимость (поддержка обоих ключей) или миграцию

2. **Обработка selectable_rules**:
   - Добавить поддержку формата `selectable_rules` с полями `label`, `enabled`, `selected_outbound` (как в `state.json`)
   - Обеспечить применение `enabled` и `selected_outbound` к соответствующим правилам из основного шаблона

3. **Обработка custom_rules (планируется)**:
   - **⚠️ Планируемая задача:** Добавить поддержку формата `custom_rules` с полной структурой `PersistedCustomRule` (как в `state.json`)
   - **⚠️ Планируемая задача:** Обеспечить загрузку пользовательских правил из `get_free.json` при инициализации визарда
   - **⚠️ Планируемая задача:** Правила должны загружаться полностью (включая `rule`, `description`, `type` и т.д.)

4. **Интеграция с визардом**:
   - При загрузке `get_free.json` применять настройки из `selectable_rules`
   - **Планируется:** При загрузке `get_free.json` загружать пользовательские правила из `custom_rules` (задача не реализована)
   - Использовать `label` для сопоставления с правилами из основного шаблона (для `selectable_rules`)
   - Применять `enabled` и `selected_outbound` к правилам с соответствующим `label`
   - Структура должна быть совместима с загрузкой из `state.json` для единообразия

## Критерии приёмки

1. ✅ Ключ `ParserConfig` переименован в `parser_config` в `get_free.json`
2. ✅ Секция `selectable_rules` добавлена с правилом "Russia-only services"
3. ✅ В `selectable_rules` используются только поля `label`, `enabled`, `selected_outbound` (как в `state.json`)
4. ✅ Структура `selectable_rules` идентична структуре `selectable_rule_states` из `state.json`
5. ⚠️ **Планируется:** Секция `custom_rules` (задача не реализована)
6. ⚠️ **Планируется:** Структура `custom_rules` идентична структуре `custom_rules` из `state.json` (полная структура `PersistedCustomRule`) (задача не реализована)
7. ✅ Код корректно парсит новую структуру `get_free.json` (с поддержкой обратной совместимости для старого формата `ParserConfig`)
8. ✅ При загрузке "Get free VPN" применяются настройки из `selectable_rules`
9. ⚠️ **Планируется:** При загрузке "Get free VPN" загружаются пользовательские правила из `custom_rules` (задача не реализована)
10. ✅ Правило "Russia-only services" имеет `enabled: true` и `selected_outbound: "ru VPN 🇷🇺"`
11. ✅ Структура согласована с `wizard_template.json` (используется ключ `parser_config`)
12. ✅ Структура `selectable_rules` согласована с `state.json`
13. ⚠️ **Планируется:** Структура `custom_rules` согласована с `state.json` (задача не реализована)

## Пример целевой структуры

```json
{
  "get_free": {
    "text": "Thank @igareck for providing VPN lists:",
    "link": "https://github.com/igareck/vpn-configs-for-russia"
  },
  "parser_config": {
    "version": 4,
    "proxies": [...],
    "outbounds": [...],
    "parser": {...}
  },
  "selectable_rules": [
    {
      "label": "Russia-only services",
      "enabled": true,
      "selected_outbound": "ru VPN 🇷🇺"
    }
  ]
}
```

**Примечание:** 
- Секция `selectable_rules` реализована и используется
- Секция `custom_rules` **планируется** (не реализована в текущей версии). В будущем может быть пустым массивом `[]`, если пользовательские правила не требуются в контексте "Get free VPN".

## Примечания

- `get_free.json` остается упрощенным шаблоном для быстрой настройки
- Не нужно дублировать всю сложность `wizard_template.json`
- `selectable_rules` в `get_free.json` — это настройки по умолчанию, которые применяются при загрузке
- **Планируется:** `custom_rules` в `get_free.json` — это пользовательские правила по умолчанию, которые загружаются при инициализации (задача не реализована)
- Структура `selectable_rules` идентична `selectable_rule_states` из `state.json` для единообразия
- **Планируется:** Структура `custom_rules` идентична `custom_rules` из `state.json` для единообразия (задача не реализована)
- Основной шаблон (`wizard_template.json`) остается источником полных определений правил
- При загрузке "Get free VPN" структура `get_free.json` должна обрабатываться так же, как `state.json`, для обеспечения единообразия

## Отчет о реализации

### Архитектура решения

Функционал работы с `get_free.json` полностью вынесен в отдельный модуль `ui/wizard/dialogs/get_free_dialog.go`, что обеспечивает:
- **Разделение ответственности**: UI-код в `source_tab.go` не содержит логики работы с файлом
- **Инкапсуляцию**: Все детали работы с `get_free.json` скрыты внутри диалога
- **Переиспользование**: Диалог может быть вызван из любого места в коде

### Поток работы

#### 1. Инициация загрузки

**Точка входа**: `tabs/source_tab.go` → кнопка "Get free VPN!" → `wizarddialogs.ShowGetFreeVPNDialog(presenter)`

```go
getFreeVPNButton := widget.NewButton("Get free VPN!", func() {
    wizarddialogs.ShowGetFreeVPNDialog(presenter)
})
```

#### 2. Загрузка файла

**Функция**: `downloadGetFreeJSON(presenter, force bool)`

**Логика**:
- Проверяет наличие локального файла `bin/get_free.json`
- Если файл отсутствует или `force=true`, скачивает с GitHub:
  - URL: `https://raw.githubusercontent.com/Leadaxe/singbox-launcher/main/bin/get_free.json`
  - Таймаут: 30 секунд
  - Лимит размера: 1 MB
  - Валидация JSON перед записью
- Если файл уже существует локально и `force=false`, пропускает скачивание

**Особенности**:
- Автоматическое создание директории `bin/` при необходимости
- Валидация JSON перед записью (защита от поврежденных данных)
- Обработка сетевых ошибок с понятными сообщениями

#### 3. Парсинг данных

**Функция**: `loadGetFreeJSON(presenter)`

**Структура данных** (`GetFreeData`):
```go
type GetFreeData struct {
    GetFree struct {
        Text string `json:"text"`
        Link string `json:"link"`
    } `json:"get_free"`
    ParserConfig json.RawMessage `json:"parser_config"`
    SelectableRules []struct {
        Label            string `json:"label"`
        Enabled          bool   `json:"enabled"`
        SelectedOutbound string `json:"selected_outbound"`
    } `json:"selectable_rules,omitempty"`
    CustomRules  []wizardmodels.PersistedCustomRule `json:"custom_rules,omitempty"`
    ConfigParams []wizardmodels.ConfigParam         `json:"config_params,omitempty"`
}
```

**Логика**:
- Читает локальный файл `bin/get_free.json`
- Если файл отсутствует, вызывает `downloadGetFreeJSON(false)` для скачивания
- Парсит JSON в структуру `GetFreeData`
- При ошибке парсинга пытается перескачать файл (`downloadGetFreeJSON(true)`)
- Валидирует наличие обязательного поля `parser_config`

**Обработка ошибок**:
- Если файл поврежден (невалидный JSON), автоматически перескачивает
- Все ошибки оборачиваются с контекстом для отладки

#### 4. Преобразование в WizardStateFile

**Функция**: `convertGetFreeDataToStateFile(getFreeData *GetFreeData)`

**Логика**:
- Преобразует `selectable_rules` из `get_free.json` в `PersistedSelectableRuleState`
- Использует фабрику `wizardmodels.NewWizardStateFile()` для создания состояния
- Фабрика инкапсулирует:
  - Парсинг `parser_config` (упрощенная структура без обертки `ParserConfig`)
  - Оборачивание в `config.ParserConfig` для внутреннего использования
  - Инициализацию пустых slice при `nil` значениях (`configParams`, `selectableRuleStates`, `customRules`)
  - Установку метаданных (`version`, `created_at`, `updated_at`)

**Преимущества фабрики**:
- Детали работы с `ParserConfig` скрыты от UI-кода
- Единая точка создания `WizardStateFile` из разных источников
- Автоматическая обработка edge cases (nil значения, пустые данные)

#### 5. Применение к модели визарда

**Функция**: `presenter.LoadState(stateFile)`

**Логика** (аналогична загрузке из `state.json`):
1. **Валидация шаблона**: Проверяет наличие `TemplateData` в модели
2. **Восстановление parser_config**: 
   - Парсит `parser_config` в `config.ParserConfig`
   - Сериализует в JSON строку для отображения в UI
   - Обновляет `model.ParserConfig` и `model.ParserConfigJSON`
3. **Извлечение SourceURLs**: Извлекает URLs из `proxies` в `ParserConfig`
4. **Восстановление config_params**: Применяет параметры конфигурации (route.final и др.)
5. **Восстановление SelectableRuleStates**:
   - Сопоставляет правила из `get_free.json` с правилами из шаблона по `label`
   - Применяет `enabled` и `selected_outbound` к соответствующим правилам
   - Правила без совпадения в шаблоне игнорируются
   - Новые правила из шаблона получают значения по умолчанию
6. **Восстановление CustomRules**: Загружает пользовательские правила (если есть)
7. **Установка флагов**: Помечает модель для перепарсинга и обновления preview
8. **Синхронизация GUI**: Обновляет все UI элементы в соответствии с моделью

#### 6. Отображение диалога

**Функция**: `ShowGetFreeVPNDialog(presenter)`

**UI компоненты**:
- **Информационные поля**: Отображает `get_free.text` и `get_free.link` (гиперссылка)
- **Кнопка "Apply configuration"**: Применяет загруженную конфигурацию к визарду
- **Кнопка "Close"**: Закрывает диалог без применения

**Асинхронная загрузка**:
- Показывает индикатор загрузки во время скачивания/парсинга
- Выполняет загрузку в отдельной горутине
- Обновляет UI через `fyne.Do()` для потокобезопасности
- Обрабатывает панику с восстановлением UI

### Ключевые особенности реализации

#### 1. Инкапсуляция логики

Вся логика работы с `get_free.json` инкапсулирована в `get_free_dialog.go`:
- **Скачивание**: `downloadGetFreeJSON()` — скрывает детали HTTP-запросов
- **Загрузка**: `loadGetFreeJSON()` — скрывает детали парсинга и валидации
- **Преобразование**: `convertGetFreeDataToStateFile()` — скрывает детали создания `WizardStateFile`
- **Фабрика**: `wizardmodels.NewWizardStateFile()` — скрывает детали работы с `ParserConfig`

#### 2. Единообразие с state.json

`get_free.json` обрабатывается через ту же логику, что и `state.json`:
- Использует `WizardStateFile` для представления данных
- Применяется через `presenter.LoadState()` — тот же метод, что и для загрузки состояний
- Структура `selectable_rules` идентична `selectable_rule_states` из `state.json`
- Обеспечивает консистентность между сохраненными состояниями и настройками по умолчанию

#### 3. Обработка nil значений

Фабрика `NewWizardStateFile()` автоматически обрабатывает `nil` значения:
- Если `configParams == nil` → инициализирует пустым slice
- Если `selectableRuleStates == nil` → инициализирует пустым slice
- Если `customRules == nil` → инициализирует пустым slice
- Это позволяет безопасно передавать данные из `get_free.json`, где эти поля могут отсутствовать

#### 4. Отсутствие дефолтных значений

Все значения берутся только из файла `get_free.json`:
- Если поля `text` или `link` пустые, они остаются пустыми (без дефолтов)
- Это обеспечивает прозрачность: пользователь видит именно то, что в файле
- Исключение: если файл уже был локально и в нем нет полей — это нормально (старый формат)

#### 5. Обработка ошибок

Все ошибки обрабатываются с контекстом:
- Сетевые ошибки: детальное сообщение с проверкой подключения
- Ошибки парсинга: автоматическая попытка перескачать файл
- Ошибки валидации: понятные сообщения для пользователя
- Паника: перехватывается и логируется, UI восстанавливается

### Интеграция с визардом

#### Сопоставление правил

При применении `selectable_rules` из `get_free.json`:
1. Создается индекс правил из `get_free.json` по `label`
2. Для каждого правила из шаблона ищется соответствующее правило из `get_free.json`
3. Если найдено:
   - Применяется `enabled` из `get_free.json`
   - Применяется `selected_outbound` из `get_free.json`
4. Если не найдено:
   - Используются значения по умолчанию из шаблона (`IsDefault`, `DefaultOutbound`)

**Пример**:
- В шаблоне есть правило с `label: "Russia-only services"`
- В `get_free.json` есть правило с `label: "Russia-only services"`, `enabled: true`, `selected_outbound: "ru VPN 🇷🇺"`
- При загрузке "Get free VPN" правило из шаблона получает `enabled=true` и `selected_outbound="ru VPN 🇷🇺"`

### Файловая структура

```
bin/
  └── get_free.json          # Локальный файл (скачивается при необходимости)
```

**Расположение**: `<execDir>/bin/get_free.json`

**Источник**: `https://raw.githubusercontent.com/Leadaxe/singbox-launcher/main/bin/get_free.json`

### Пример использования

1. Пользователь нажимает кнопку "Get free VPN!" в табе "Sources & ParserConfig"
2. Показывается индикатор загрузки "Downloading get_free.json..."
3. В фоне:
   - Проверяется наличие локального файла
   - Если отсутствует, скачивается с GitHub
   - Файл парсится в структуру `GetFreeData`
   - Данные преобразуются в `WizardStateFile`
4. Показывается диалог с:
   - Текстом благодарности (`get_free.text`)
   - Гиперссылкой (`get_free.link`)
   - Кнопкой "Apply configuration"
5. При нажатии "Apply configuration":
   - Вызывается `presenter.LoadState(stateFile)`
   - Применяется `parser_config` к модели
   - Применяются настройки из `selectable_rules`
   - Обновляется UI визарда
   - Показывается сообщение об успехе
6. Диалог закрывается, пользователь видит примененную конфигурацию

### Преимущества реализации

1. **Модульность**: Весь функционал в одном файле, легко поддерживать
2. **Переиспользование**: Использует существующую инфраструктуру (`LoadState`, `WizardStateFile`)
3. **Единообразие**: Та же логика, что и для загрузки состояний
4. **Надежность**: Обработка всех edge cases (nil, ошибки, паника)
5. **Прозрачность**: Все значения из файла, без скрытых дефолтов
6. **Инкапсуляция**: Детали работы с `ParserConfig` скрыты в фабрике

