# Проверка обновлений с попапом при запуске

**Дата создания:** 2026-02-10  
**Дата завершения:** 2026-02-10  
**Статус:** ✅ Завершено

---

## 📋 Краткое описание

Реализовать функционал автоматической проверки обновлений при запуске приложения с отображением попапа, если доступна новая версия. Попап должен показывать информацию об обновлении и ссылки на скачивание новой версии.

---

## 🎯 Цели и требования

### Основные требования

1. **Автоматическая проверка при запуске:**
   - При запуске приложения должна выполняться проверка наличия новой версии
   - Проверка выполняется асинхронно, не блокируя запуск приложения
   - Используется существующая функция `CheckLauncherVersionOnStartup()` или расширяется её функциональность

2. **Попап с информацией об обновлении:**
   - Если доступна новая версия, показывается попап (диалог) с информацией
   - Попап содержит:
     - Текущую установленную версию
     - Доступную новую версию
     - Ссылки на скачивание новой версии (GitHub Releases)
   - Попап не блокирует работу приложения (модальный, но не критичный)

3. **Условия показа попапа:**
   - Попап показывается при первом отображении главного окна (через `OnWindowShown` callback)
   - Показывается только один раз за сессию (если пользователь закрыл попап, повторно не показывать)
   - Показывается только если есть обновление (текущая версия < последняя версия)

4. **Логика показа:**
   - Проверка версии всегда выполняется при старте приложения (независимо от флага `-tray`)
   - Результат проверки сохраняется в кеш
   - Попап показывается при первом открытии окна через `OnWindowShown` callback
   - Если окно уже открыто при старте, попап показывается сразу после проверки

---

## 🔧 Технические детали

### Используемые компоненты

1. **Проверка версии:**
   - `GetLatestLauncherVersion()` - получение последней версии с GitHub
   - `GetCachedLauncherVersion()` - получение закешированной версии
   - `CompareVersions()` - сравнение версий
   - `constants.AppVersion` - текущая версия приложения

2. **UI компоненты:**
   - `fyne.Do()` - для безопасного обновления UI из горутин
   - `components.NewCustom()` - для отображения попапа (из `ui/components/custom_dialog.go`)
   - `widget.NewHyperlink()` - для ссылок на скачивание
   - UI-код находится в `ui/core_dashboard_tab.go` (метод `showUpdatePopup`)

3. **Параметры запуска:**
   - `-tray` флаг из `main.go` - определяет, нужно ли скрывать окно при запуске

4. **Открытие главного окна:**
   - `ShowMainWindowOrFocusWizard()` - функция для открытия главного окна
   - Вызывается из трей-меню и при клике на иконку в Dock (macOS)

### Структура данных

**Информация об обновлении:**
```go
type UpdateInfo struct {
    CurrentVersion string
    LatestVersion  string
    UpdateAvailable bool
    DownloadURL    string // URL на страницу релизов GitHub
}
```

### URL для скачивания

- **GitHub Releases:** `https://github.com/Leadaxe/singbox-launcher/releases/latest`
- Или прямая ссылка на последний релиз (если доступна через API)

---

## 📐 Архитектура решения

### 1. CheckLauncherVersionOnStartup

**Поведение:**
- Проверяет версию при старте (всегда, независимо от флага `-tray`)
- Сохраняет результат в кеш
- Не показывает UI напрямую
- Попап показывается через `OnWindowShown` callback при первом открытии окна

### 2. Метод для показа попапа

**Метод:** `ShowUpdatePopupIfAvailable()`

**Логика:**
1. Проверяет, не был ли попап уже показан в этой сессии
2. Получает текущую версию из `constants.AppVersion`
3. Получает последнюю версию из кеша
4. Сравнивает версии через `CompareVersions()`
5. Если есть обновление:
   - Вызывает callback `ShowUpdatePopupFunc` из `UIService`
   - Callback реализован в `ui/core_dashboard_tab.go` (метод `showUpdatePopup`)

### 3. Интеграция с открытием окна

**Callback `OnWindowShown` в `UIService`:**
- Вызывается после открытия главного окна в `ShowMainWindowOrFocusWizard()`
- Вызывает `ShowUpdatePopupIfAvailable()` для проверки и показа попапа
- Устанавливается в `NewAppController()` в `core/controller.go`

### 4. Управление состоянием

**Поля в AppController:**
- `updatePopupShown bool` - флаг, что попап уже был показан в этой сессии
- `updatePopupMutex sync.RWMutex` - мьютекс для защиты `updatePopupShown`
- Методы: `SetUpdatePopupShown(bool)`, `isUpdatePopupShown() bool` (приватный)

---

## 🎨 UI/UX требования

### Дизайн попапа

1. **Заголовок:** "Update Available"
2. **Содержимое:**
   - Текст: "A new version of the application is available"
   - Текущая версия: `v0.6.0` (пример)
   - Новая версия: `v0.7.0` (пример)
   - Ссылка на скачивание: "Download from GitHub" (гиперссылка)
3. **Кнопки:**
   - "Close" - закрывает попап (создается через `components.NewCustom`)
   - Кнопки "Download" нет, только гиперссылка

### Поведение

- Попап модальный, но не блокирующий (можно закрыть и продолжить работу)
- При клике на ссылку открывается браузер с GitHub Releases
- Попап показывается поверх главного окна (если оно открыто)

---

## 🔄 Сценарии использования

### Сценарий 1: Запуск без `-tray`, есть обновление

1. Пользователь запускает приложение без параметра `-tray`
2. Приложение проверяет версию в фоне и сохраняет в кеш
3. Окно открывается сразу
4. При первом отображении окна (через `OnWindowShown`) показывается попап
5. Пользователь видит информацию об обновлении и ссылку

### Сценарий 2: Запуск с `-tray`, есть обновление

1. Пользователь запускает приложение с параметром `-tray`
2. Приложение проверяет версию в фоне и сохраняет в кеш
3. Окно скрыто при старте
4. Пользователь открывает окно через трей
5. При первом отображении окна (через `OnWindowShown`) показывается попап

### Сценарий 3: Запуск без `-tray`, нет обновления

1. Пользователь запускает приложение без параметра `-tray`
2. Приложение проверяет версию в фоне и сохраняет в кеш
3. Обновления нет, попап не показывается
4. Приложение работает как обычно

### Сценарий 4: Запуск с `-tray`, нет обновления

1. Пользователь запускает приложение с параметром `-tray`
2. Приложение проверяет версию в фоне и сохраняет в кеш
3. Обновления нет, попап не показывается
4. При открытии окна через трей попап не показывается

### Сценарий 5: Повторное открытие окна

1. Пользователь закрыл попап
2. Пользователь закрыл окно
3. Пользователь открывает окно снова
4. Попап НЕ показывается повторно (уже был показан в этой сессии)

---

## 🛠️ Реализация

### Шаг 1: Модифицировать CheckLauncherVersionOnStartup

**Файл:** `core/core_version.go`

**Изменить сигнатуру функции:**
```go
// CheckLauncherVersionOnStartup выполняет разовую проверку версии launcher при старте
// Проверка всегда выполняется и сохраняет результат в кеш.
// Попап с обновлением показывается при первом отображении окна (через OnWindowShown).
func (ac *AppController) CheckLauncherVersionOnStartup() {
    // Проверяет версию в фоне
    // Сохраняет результат в кеш
    // Не показывает UI напрямую
}
```

### Шаг 2: Добавить метод показа попапа

**Файл:** `core/core_version.go`

```go
// ShowUpdatePopupIfAvailable проверяет наличие обновления и показывает попап
func (ac *AppController) ShowUpdatePopupIfAvailable() {
    // Проверяет, не был ли попап уже показан
    // Получает текущую и последнюю версию
    // Сравнивает версии
    // Если есть обновление, вызывает callback ShowUpdatePopupFunc
}
```

### Шаг 3: Интегрировать с открытием окна

**Файл:** `core/services/ui_service.go`

**Добавить callback в UIService:**
```go
type UIService struct {
    // ... существующие поля
    OnWindowShown func() // Вызывается после открытия главного окна
    ShowUpdatePopupFunc func(currentVersion, latestVersion string) // Callback для показа попапа
}
```

**Модифицировать `ShowMainWindowOrFocusWizard()`:**
```go
func (ui *UIService) ShowMainWindowOrFocusWizard() {
    // ... существующий код ...
    
    // Вызываем callback после открытия окна (для проверки обновлений)
    if ui.OnWindowShown != nil {
        ui.OnWindowShown()
    }
}
```

**Файл:** `core/controller.go`

**Установить callback в NewAppController:**
```go
// В функции NewAppController, после создания UIService
ac.UIService.OnWindowShown = func() {
    ac.ShowUpdatePopupIfAvailable()
}
```

### Шаг 4: Реализовать UI для попапа

**Файл:** `ui/core_dashboard_tab.go`

**Добавить метод `showUpdatePopup`:**
```go
func (tab *CoreDashboardTab) showUpdatePopup(currentVersion, latestVersion string) {
    // Устанавливает флаг, что попап был показан
    // Создает содержимое попапа с информацией об обновлении
    // Использует components.NewCustom для отображения диалога
    // Показывает только ссылку "Download from GitHub" и кнопку "Close"
}
```

**Установить callback в `CreateCoreDashboardTab`:**
```go
// Регистрируем callback для показа попапа обновления
tab.controller.UIService.ShowUpdatePopupFunc = tab.showUpdatePopup
```

### Шаг 5: Управление состоянием показа попапа

**Файл:** `core/controller.go`

**Добавить поля в AppController:**
```go
type AppController struct {
    // ... существующие поля
    updatePopupShown bool         // Флаг, что попап уже был показан в этой сессии
    updatePopupMutex sync.RWMutex // Мьютекс для защиты updatePopupShown
}
```

**Добавить методы:**
```go
// SetUpdatePopupShown устанавливает флаг, что попап обновления был показан
func (ac *AppController) SetUpdatePopupShown(shown bool) {
    ac.updatePopupMutex.Lock()
    defer ac.updatePopupMutex.Unlock()
    ac.updatePopupShown = shown
}

// isUpdatePopupShown возвращает, был ли попап обновления показан
func (ac *AppController) isUpdatePopupShown() bool {
    ac.updatePopupMutex.RLock()
    defer ac.updatePopupMutex.RUnlock()
    return ac.updatePopupShown
}
```

---

## ✅ Критерии приемки

1. ✅ Проверка версии всегда выполняется при старте (независимо от флага `-tray`)
2. ✅ Попап показывается при первом отображении окна через `OnWindowShown` callback
3. ✅ Попап содержит текущую и новую версию
4. ✅ Попап содержит ссылку на скачивание новой версии (гиперссылка "Download from GitHub")
5. ✅ Попап показывается только один раз за сессию
6. ✅ При отсутствии обновления попап не показывается
7. ✅ Проверка версии не блокирует запуск приложения
8. ✅ Все ошибки обрабатываются корректно (нет обновления = не ошибка)
9. ✅ UI-код отделен от бизнес-логики (попап в `ui/core_dashboard_tab.go`)
10. ✅ Используется `components.NewCustom` для отображения диалога
11. ✅ Все тексты на английском языке

---

## 🧪 Тестирование

### Тестовые сценарии

1. **Запуск без `-tray`, есть обновление:**
   - Запустить приложение
   - Проверить, что попап появился
   - Проверить содержимое попапа

2. **Запуск с `-tray`, есть обновление:**
   - Запустить с `-tray`
   - Проверить, что попап НЕ появился при старте
   - Открыть окно через трей
   - Проверить, что попап появился

3. **Нет обновления:**
   - Запустить приложение (текущая версия = последняя)
   - Проверить, что попап НЕ появился

4. **Повторное открытие:**
   - Показать попап
   - Закрыть попап
   - Закрыть окно
   - Открыть окно снова
   - Проверить, что попап НЕ появился повторно

5. **Ошибка проверки версии:**
   - Отключить интернет
   - Запустить приложение
   - Проверить, что приложение работает нормально (попап не показывается при ошибке)

---

## 📝 Примечания

### Обработка ошибок

- Если проверка версии не удалась (нет интернета, таймаут), попап НЕ показывается
- Ошибки логируются через `debuglog`, но не показываются пользователю
- Попап показывается только при успешной проверке и наличии обновления

### Производительность

- Проверка версии выполняется асинхронно в горутине
- Не блокирует запуск приложения
- Используется кешированная версия, если доступна

### Совместимость

- Работает на всех поддерживаемых платформах (Windows, Linux, macOS)
- Использует существующие компоненты Fyne для UI
- Не требует новых зависимостей

---

## 🔗 Связанные файлы

- `main.go` - точка входа, вызов `CheckLauncherVersionOnStartup()`
- `core/core_version.go` - проверка версий, `ShowUpdatePopupIfAvailable()`
- `core/controller.go` - управление состоянием попапа, установка `OnWindowShown` callback
- `core/services/ui_service.go` - `OnWindowShown` callback, `ShowUpdatePopupFunc` callback
- `ui/core_dashboard_tab.go` - реализация `showUpdatePopup()`, UI попапа
- `ui/components/custom_dialog.go` - компонент для отображения диалога
- `internal/constants/constants.go` - текущая версия приложения

---

## 📚 Ссылки

- GitHub Releases: `https://github.com/Leadaxe/singbox-launcher/releases/latest`
- GitHub API: `https://api.github.com/repos/Leadaxe/singbox-launcher/releases/latest`
- Fyne Dialog Documentation: `https://developer.fyne.io/api/v2.4/dialog`

