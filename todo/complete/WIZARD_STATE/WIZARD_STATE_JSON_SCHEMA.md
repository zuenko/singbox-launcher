# Схема файла state.json

## Общая структура

```json
{
  "version": 1,
  "comment": "Описание состояния (опционально)",
  "created_at": "2026-01-30T12:00:00Z",
  "updated_at": "2026-01-30T12:00:00Z",
  
  "parser_config": {
    "ParserConfig": {
      "version": 4,
      "proxies": [...],
      "outbounds": [...],
      "parser": {...}
    }
  },
  "config_params": [
    {
      "name": "route.final",
      "value": "proxy-out"
    },
    {
      "name": "experimental.clash_api.secret",
      "value": "generated-secret-token"
    }
  ],
  "selectable_rule_states": [...],
  "custom_rules": [...]
}

---

## Детальное описание полей

### 1. Метаданные состояния

#### `version` (int, обязательное)
- **Тип:** `integer`
- **Значение:** `1` (в первой версии)
- **Описание:** Версия формата файла состояния. Используется для будущих миграций.

#### `id` (string, опциональное)
- **Тип:** `string`
- **Обязательность:** 
  - Для `state.json` — **не требуется** (имя файла является идентификатором)
  - Для именованных состояний (`<id>.json`) — **обязательно** и должно совпадать с именем файла (без `.json`)
- **Валидация:** 
  - Только буквы (a-z, A-Z), цифры (0-9), дефис (-), подчёркивание (_)
  - Максимальная длина: 50 символов
  - Должно совпадать с именем файла (для именованных состояний)
- **Пример:** 
  - Для `state.json`: поле отсутствует или `null`
  - Для `my-config.json`: `"my-config"`
  - Для `work-vpn.json`: `"work-vpn"`

#### `comment` (string, опциональное)
- **Тип:** `string`
- **Описание:** Комментарий/описание состояния для пользователя. Может использоваться в UI для отображения дополнительной информации о состоянии.
- **Пример:** `"Конфигурация для работы"`, `"Домашняя сеть с блокировкой рекламы"`, `""` (пустая строка) или поле отсутствует

#### `created_at` (string, обязательное)
- **Тип:** `string` (RFC3339)
- **Описание:** Дата и время создания состояния в формате RFC3339 (UTC)
- **Пример:** `"2026-01-30T12:00:00Z"`

#### `updated_at` (string, обязательное)
- **Тип:** `string` (RFC3339)
- **Описание:** Дата и время последнего обновления состояния в формате RFC3339 (UTC)
- **Пример:** `"2026-01-30T15:30:00Z"`

---

### 2. ParserConfig данные

#### `parser_config` (object, обязательное)
- **Тип:** `object` (JSON объект)
- **Описание:** Полная конфигурация парсера в формате JSON объекта. Это единственный источник `@ParserConfig` для всего приложения:
  - Используется визардом при работе с конфигурацией
  - Используется при нажатии кнопки "🔄 Update" в главном окне для обновления конфигурации из подписок и для регулярных обновлений
  - При сохранении финальной конфигурации этот блок включается в `config.json` как комментарий `/** @ParserConfig */`
- **Важно:** `@ParserConfig` больше не хранится в `config.json` (только в `state.json`). При загрузке состояния визард и функция обновления используют этот блок вместо `config.json`.
- **Структура:** JSON объект с полем `ParserConfig`:
  ```json
  {
    "ParserConfig": {
      "version": 4,
      "proxies": [
        {
          "source": "https://example.com/sub.txt"
        }
      ],
      "outbounds": [],
      "parser": {
        "reload": "1h",
        "last_updated": "2026-01-30T12:00:00Z"
      }
    }
  }
  ```

**Важно:** 
- Это единственный источник `@ParserConfig` для визарда
- При загрузке состояния визард использует этот блок вместо `config.json`
- При сохранении финальной конфигурации этот блок больше не копируется в `config.json` как комментарий, остается только тут

---

**Примечание:** URL источников прокси хранятся в `parser_config.ParserConfig.proxies` (поля `source` и `connections`). При восстановлении состояния URL'ы извлекаются из `parser_config.Proxies` обратно в текстовый формат для отображения в UI.

---

### 4. Параметры конфигурации

#### `config_params` (array, обязательное)
- **Тип:** `array` of `ConfigParam`
- **Описание:** Параметры конфигурации, используемые при генерации финального конфига. Каждый параметр имеет имя (путь в JSON) и значение.
- **Структура элемента:**
  ```json
  {
    "name": "route.final",
    "value": "proxy-out"
  }
  ```
- **Поля элемента:**
  - `name` (string, обязательное) — путь к параметру в конфигурации, используя точечную нотацию (например, `"route.final"`, `"experimental.clash_api.secret"`)
  - `value` (string, обязательное) — значение параметра (может быть пустой строкой `""` для использования значения по умолчанию из шаблона)

**Стандартные параметры:**
- `route.final` — финальный outbound по умолчанию для правил маршрутизации (используется, если правило не имеет явного outbound)
- `experimental.clash_api.secret` — секретный токен для Clash API. Если отсутствует при сохранении конфига, генерируется случайный, но затем сохраняется в `state.json` для последующего использования

**Пример:**
```json
[
  {
    "name": "route.final",
    "value": "proxy-out"
  },
  {
    "name": "experimental.clash_api.secret",
    "value": "my-secret-token-12345"
  }
]
```

**Важно:**
- Шаблон используется всегда при генерации конфига (секции шаблона определяют структуру)
- Параметры из `config_params` применяются к шаблону при генерации
- Если параметр отсутствует в `config_params`, используется значение по умолчанию из шаблона (если есть)
- Параметр `experimental.clash_api.secret` генерируется автоматически при первом сохранении, если отсутствует, но затем сохраняется в `state.json`

---

### 5. Состояния правил из шаблона

#### `selectable_rule_states` (array, обязательное)
- **Тип:** `array` of `PersistedRuleState`
- **Описание:** Состояния правил маршрутизации, которые были извлечены из шаблона (`@SelectableRule` блоки). Каждый элемент содержит исходное правило из шаблона и состояние (включено/выключено, выбранный outbound).

**Структура элемента `PersistedRuleState`:**
```json
{
  "type": "System",
  "rule": {
    "label": "Block ads",
    "description": "Block advertisement domains",
    "raw": {
      "domain_suffix": ["ads.example.com"],
      "action": "reject"
    },
    "default_outbound": "reject",
    "has_outbound": false,
    "is_default": true
  },
  "enabled": true,
  "selected_outbound": "reject"
}
```

**Поля `PersistedRuleState`:**
- `type` (string, обязательное) — тип правила для отображения в UI:
  - `"System"` — системное/предустановленное правило из шаблона (можно только включать/выключать, нельзя редактировать)
  - `"IP Addresses (CIDR)"` — пользовательское правило с IP адресами (можно редактировать и изменять тип)
  - `"Domains/URLs"` — пользовательское правило с доменами (можно редактировать и изменять тип)
  - `"Processes"` — пользовательское правило с процессами (можно редактировать и изменять тип)
  - `"Custom JSON"` — пользовательское правило с произвольным JSON (можно редактировать и изменять тип)
- `rule` (object, обязательное) — исходное правило из шаблона:
  - `label` (string) — название правила
  - `description` (string) — описание правила
  - `raw` (object) — JSON объект правила (domain, domain_suffix, ip, geoip, и т.д.)
  - `default_outbound` (string) — outbound по умолчанию из шаблона
  - `has_outbound` (boolean) — есть ли поле outbound в правиле
  - `is_default` (boolean) — должно ли правило быть включено по умолчанию
- `enabled` (boolean, обязательное) — включено ли правило в финальной конфигурации
- `selected_outbound` (string, обязательное) — выбранный outbound для правила (может быть "reject", "drop", или имя outbound)

**Определение типа:**
- **Для `selectable_rule_states` (предустановленные из шаблона):**
  - По умолчанию поле `type` равно `"System"` — это означает, что правило системное и его нельзя редактировать
  - Системные правила (`type = "System"`) можно только включать/выключать (`enabled`) и выбирать outbound (`selected_outbound`), но нельзя редактировать (`rule.raw`, `label`, `description` нельзя изменять)
  - **Правило можно сделать редактируемым:** если пользователь изменяет `type` на один из редактируемых типов (`"IP Addresses (CIDR)"`, `"Domains/URLs"`, `"Processes"`, `"Custom JSON"`), то правило становится редактируемым
  - При изменении типа на редактируемый правило можно полностью редактировать (изменять `rule.raw`, `label`, `description`, `type`)
  - Тип правила для отображения в UI определяется из `rule.raw` по следующей логике (если `type = "System"`):
    1. Если есть поле `ip_cidr` → отображается как "IP Addresses (CIDR)"
    2. Если есть поле `domain_regex` → отображается как "Domains/URLs"
    3. Если есть поле `domain`, `domain_suffix`, `domain_keyword` → отображается как "Domains/URLs"
    4. Если есть поле `process_name` → отображается как "Processes"
    5. Иначе → отображается как "Custom JSON"
- **Для `custom_rules` (пользовательские):**
  - Поле `type` сохраняется явно, так как пользователь выбирает его при создании правила
  - Возможные значения: `"IP Addresses (CIDR)"`, `"Domains/URLs"`, `"Processes"`, `"Custom JSON"`
  - **Тип можно изменять** при редактировании правила
  - Правило можно полностью редактировать (изменять `rule.raw`, `label`, `description`, `type`)

**Пример массива:**
```json
[
  {
    "type": "System",
    "rule": {
      "label": "Block ads",
      "description": "Block advertisement domains",
      "raw": {
        "domain_suffix": ["ads.example.com"],
        "action": "reject"
      },
      "default_outbound": "reject",
      "has_outbound": false,
      "is_default": true
    },
    "enabled": true,
    "selected_outbound": "reject"
  },
  {
    "type": "System",
    "rule": {
      "label": "Route example.com",
      "description": "Route example.com to proxy",
      "raw": {
        "domain": ["example.com"],
        "outbound": "proxy-out"
      },
      "default_outbound": "proxy-out",
      "has_outbound": true,
      "is_default": false
    },
    "enabled": true,
    "selected_outbound": "proxy-out"
  }
]
```

**Важно:**
- **По умолчанию (режим редактирования):** При загрузке существующего `state.json` все данные (включая `rule.raw`, `label`, `description`) берутся из файла состояния. Шаблон используется только для генерации финального конфига (структура и секции).
- **Режим "Настроить заново":** Опция в UI, которая игнорирует `state.json` и использует только шаблон для создания нового состояния с нуля. Полезно, когда шаблон обновился и нужно начать с чистого листа.
- Для предустановленных правил (`selectable_rule_states`):
  - По умолчанию `type` равен `"System"` — правило системное, нельзя редактировать
  - Если `type = "System"`: можно только включать/выключать (`enabled`) и выбирать outbound (`selected_outbound`), `rule.raw`, `label`, `description` нельзя изменять
  - Если `type` изменён на редактируемый (`"IP Addresses (CIDR)"`, `"Domains/URLs"`, `"Processes"`, `"Custom JSON"`): правило становится редактируемым, можно изменять `rule.raw`, `label`, `description`, `type`
- Для пользовательских правил (`custom_rules`):
  - `type` может быть `"IP Addresses (CIDR)"`, `"Domains/URLs"`, `"Processes"`, `"Custom JSON"`
  - Правило можно полностью редактировать (изменять `rule.raw`, `label`, `description`, `type`)

---

### 6. Пользовательские правила

#### `custom_rules` (array, обязательное)
- **Тип:** `array` of `PersistedRuleState`
- **Описание:** Пользовательские правила маршрутизации, созданные пользователем вручную (не из шаблона). Структура такая же, как `selectable_rule_states`.

**Структура элемента:** Та же, что и в `selectable_rule_states`

**Пример:**
```json
[
  {
    "type": "Domains/URLs",
    "rule": {
      "label": "Custom rule",
      "description": "Route specific domain to proxy",
      "raw": {
        "domain": ["custom.example.com"],
        "outbound": "proxy-out"
      },
      "default_outbound": "proxy-out",
      "has_outbound": true,
      "is_default": false
    },
    "enabled": true,
    "selected_outbound": "proxy-out"
  }
]
```

**Важно:**
- Эти правила создаются пользователем вручную
- Они не привязаны к шаблону
- При восстановлении состояния они загружаются полностью (включая `rule.raw`)

---


## Полный пример файла state.json

```json
{
  "version": 1,
  "comment": "Текущее рабочее состояние",
  "created_at": "2026-01-30T12:00:00Z",
  "updated_at": "2026-01-30T15:30:00Z",
  
  "parser_config": {
    "ParserConfig": {
      "version": 4,
      "proxies": [
        {
          "source": "https://example.com/sub.txt"
        },
        {
          "source": "vless://uuid@server.com:443?encryption=none&security=tls&sni=server.com#Server1"
        }
      ],
      "outbounds": [],
      "parser": {
        "reload": "1h",
        "last_updated": "2026-01-30T12:00:00Z"
      }
    }
  },
  
  "config_params": [
    {
      "name": "route.final",
      "value": "proxy-out"
    },
    {
      "name": "experimental.clash_api.secret",
      "value": "my-secret-token-12345"
    }
  ],
  
  "selectable_rule_states": [
    {
      "type": "System",
      "rule": {
        "label": "Block ads",
        "description": "Block advertisement domains",
        "raw": {
          "domain_suffix": ["ads.example.com"],
          "action": "reject"
        },
        "default_outbound": "reject",
        "has_outbound": false,
        "is_default": true
      },
      "enabled": true,
      "selected_outbound": "reject"
    },
    {
      "type": "Domains/URLs",
      "rule": {
        "label": "Route example.com",
        "description": "Route example.com to proxy",
        "raw": {
          "domain": ["example.com"],
          "outbound": "proxy-out"
        },
        "default_outbound": "proxy-out",
        "has_outbound": true,
        "is_default": false
      },
      "enabled": true,
      "selected_outbound": "proxy-out"
    }
  ],
  
  "custom_rules": [
    {
      "type": "Custom JSON",
      "rule": {
        "label": "Custom rule",
        "description": "Route custom domain to proxy",
        "raw": {
          "domain": ["custom.example.com"],
          "outbound": "proxy-out"
        },
        "default_outbound": "proxy-out",
        "has_outbound": true,
        "is_default": false
      },
      "enabled": true,
      "selected_outbound": "proxy-out"
    }
  ]
}
```

---

## Что НЕ сохраняется в state.json

### TemplateData (не сохраняется)
- **Причина:** Шаблон загружается из файла (`config_template.json` или `config_template_macos.json`) всегда при генерации конфига (определяет структуру и секции)
- **Содержимое:**
  - `Sections` (map[string]json.RawMessage) — секции конфига из шаблона (route, dns, inbounds, outbounds, experimental и т.д.). **Всегда используются из шаблона** для генерации конфига
  - `SectionOrder` ([]string) — порядок секций в шаблоне. **Всегда используется из шаблона** для сохранения порядка при генерации
  - `SelectableRules` ([]TemplateSelectableRule) — правила из шаблона (из `@SelectableRule` блоков). **В режиме редактирования НЕ используются** (правила берутся из `state.json`), **в режиме "Настроить заново" используются** для инициализации
  - `DefaultFinal` (string) — финальный outbound по умолчанию из секции `route` шаблона. **Используется как fallback**, если не задан в `config_params`
  - `HasParserOutboundsBlock` (bool) — есть ли маркер `@PARSER_OUTBOUNDS_BLOCK` в шаблоне. **Всегда используется из шаблона** для определения, куда вставлять сгенерированные outbounds
  - `OutboundsAfterMarker` (string) — элементы после маркера `@PARSER_OUTBOUNDS_BLOCK` (например, `direct-out`). **Всегда используется из шаблона** для добавления после сгенерированных outbounds
- **Восстановление:** 
  - В режиме редактирования: шаблон загружается для генерации конфига (структура и секции), но правила берутся из `state.json`
  - В режиме "Настроить заново": шаблон используется для инициализации нового состояния (включая правила)

### GeneratedOutbounds (не сохраняется)
- **Причина:** Генерируются из `parser_config_json` при парсинге
- **Восстановление:** При загрузке состояния запускается парсинг, и outbounds генерируются заново

### TemplatePreviewText (не сохраняется)
- **Причина:** Это кэш для preview, не критично для восстановления состояния
- **Восстановление:** Генерируется заново при необходимости

---

## Восстановление состояния

### Режим редактирования (по умолчанию)

При загрузке `state.json` (по умолчанию):

1. **Загрузить шаблон** из файла (`config_template.json` или `config_template_macos.json`) — шаблон используется для генерации конфига (структура и секции)
2. **Восстановить ParserConfig** из `parser_config`:
   - Используется визардом для работы с конфигурацией
   - Используется при нажатии кнопки "🔄 Update" в главном окне для обновления конфигурации
   - Это единственный источник `@ParserConfig` (больше не используется `config.json`)
3. **Извлечь SourceURLs** из `parser_config.ParserConfig.proxies` (поля `source` и `connections`) для отображения в UI
4. **Восстановить параметры конфигурации** из `config_params` (например, `route.final`, `experimental.clash_api.secret`)
5. **Восстановить состояния правил:**
   - Для `selectable_rule_states`: загрузить полностью из файла (включая `rule.raw`, `label`, `description`, `type`, `enabled`, `selected_outbound`)
   - Для `custom_rules`: загрузить полностью из файла
6. **Запустить парсинг** для генерации `GeneratedOutbounds`
7. **Синхронизировать GUI** с восстановленной моделью

**Важно:** В режиме редактирования все данные берутся из `state.json`. Шаблон используется только при генерации финального конфига для определения структуры и секций.

### Режим "Настроить заново"

При выборе опции "Настроить заново" в UI:

1. **Игнорировать `state.json`** — не загружать сохраненное состояние
2. **Загрузить шаблон** из файла (`config_template.json` или `config_template_macos.json`)
3. **Инициализировать новое состояние** на основе шаблона:
   - Извлечь правила из шаблона (`@SelectableRule` блоки) в `selectable_rule_states` с дефолтными настройками (`enabled=false`, `type="System"`)
   - Инициализировать `config_params`:
     - `route.final`: значение из `TemplateData.DefaultFinal` (если есть в шаблоне), иначе пустая строка `""`
     - `experimental.clash_api.secret`: **не инициализируется** — будет сгенерирован автоматически при первом сохранении конфига (как сейчас при создании конфига из визарда)
   - Загрузить `@ParserConfig` из `config.json` или шаблона (если есть)
4. **Запустить парсинг** для генерации `GeneratedOutbounds`
5. **Синхронизировать GUI** с новой моделью

**Важно:** Режим "Настроить заново" полезен, когда шаблон обновился и нужно начать с чистого листа, игнорируя старое сохраненное состояние.

---

## Валидация при загрузке

- `version` должен быть равен `1` (в первой версии)
- `id` должен быть валидным (только разрешённые символы, макс. 50 символов)
- `parser_config` должен быть валидным JSON объектом с полем `ParserConfig`
- `config_params` должен быть массивом
- Каждый элемент `config_params` должен иметь поля `name` и `value` (оба строки)
- `selectable_rule_states` и `custom_rules` должны быть массивами
- Каждый элемент `selectable_rule_states` и `custom_rules` должен иметь поля `rule`, `enabled`, `selected_outbound`
- `rule.raw` должен быть валидным JSON объектом

---

## Размер файла

- **Максимальный размер:** 256 KB
- **Типичный размер:** 10-50 KB (зависит от количества правил)
- При превышении лимита показывается предупреждение, но сохранение не блокируется

