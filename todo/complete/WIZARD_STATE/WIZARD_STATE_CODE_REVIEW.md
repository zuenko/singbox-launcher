# Code Review: Управление состояниями визарда

**Дата:** 2026-02-09  
**Рецензент:** AI Assistant  
**Статус:** Критические проблемы обнаружены

---

## 📋 Обзор

Проведен code review реализации системы управления состояниями визарда согласно спецификации `WIZARD_STATE_MANAGEMENT_SPEC.md`. Реализация в целом соответствует спецификации, но обнаружены **критические проблемы**, которые могут привести к потере данных и некорректной работе приложения.

---

## 🔴 Критические проблемы

### 1. Кнопка "🔄 Update" не использует `state.json`

**Проблема:** Кнопка "🔄 Update" в главном окне (`core_dashboard_tab.go`) использует `ExtractParserConfig()` из `config.json`, но согласно спецификации, `@ParserConfig` больше не должен храниться в `config.json` (только в `state.json`).

**Местоположение:**
- `core/config_service.go:110` - `ExtractParserConfig(ac.FileService.ConfigPath)`
- `core/config/parser/factory.go:19` - функция читает из `config.json`

**Последствия:**
- При работе с визардом кнопка "🔄 Update" не будет находить `@ParserConfig` в `config.json`
- Обновление конфигурации из подписок не будет работать
- Пользователь потеряет функциональность автоматического обновления

**Решение:**
1. Модифицировать `ExtractParserConfig()` для проверки `state.json` перед `config.json`
2. Или создать новую функцию `ExtractParserConfigFromState()` и использовать её в `ConfigService`
3. Обновить логику в `core/config_service.go:UpdateConfigFromSubscriptions()` для использования `state.json`

**Приоритет:** 🔴 Критический

---

### 2. `experimental.clash_api.secret` не сохраняется в `state.json`

**Проблема:** При сохранении конфига через `SaveConfigWithBackup()` генерируется новый `experimental.clash_api.secret`, но он не сохраняется обратно в `state.json`. При следующем открытии визарда secret будет потерян.

**Местоположение:**
- `ui/wizard/business/saver.go:69-104` - генерация secret при сохранении конфига
- `ui/wizard/presentation/presenter_state.go:115-116` - TODO комментарий о том, что secret не сохраняется

**Последствия:**
- При каждом сохранении конфига генерируется новый secret
- При следующем открытии визарда secret теряется
- Если пользователь использует Clash API, secret будет меняться при каждом сохранении

**Решение:**
1. После сохранения конфига через `SaveConfigWithBackup()` извлечь `experimental.clash_api.secret` из сохранённого конфига
2. Обновить `config_params` в текущем состоянии визарда
3. Сохранить обновлённое состояние в `state.json`

**Приоритет:** 🔴 Критический

---

## ⚠️ Серьёзные проблемы

### 3. Неполное восстановление `config_params`

**Проблема:** В `restoreConfigParams()` есть TODO о сохранении остальных параметров (кроме `route.final`), но они не сохраняются для применения при генерации конфига.

**Местоположение:**
- `ui/wizard/presentation/presenter_state.go:274-288` - функция `restoreConfigParams()`
- `ui/wizard/presentation/presenter_state.go:286-287` - TODO комментарий

**Последствия:**
- Параметры типа `experimental.clash_api.secret` не применяются при генерации конфига
- Пользовательские параметры теряются при загрузке состояния

**Решение:**
1. Сохранять все `config_params` в модель (не только `route.final`)
2. Применять все параметры при генерации конфига в `presenter_save.go`

**Приоритет:** ⚠️ Высокий

---

### 4. Отсутствие защиты от race conditions

**Проблема:** Нет защиты от одновременного сохранения/загрузки состояний. Если пользователь быстро нажмёт "Save" несколько раз или закроет визард во время сохранения, возможна потеря данных.

**Местоположение:**
- `ui/wizard/business/state_store.go` - все методы сохранения/загрузки
- `ui/wizard/presentation/presenter_state.go` - методы `SaveCurrentState()`, `SaveStateAs()`

**Последствия:**
- Возможна потеря данных при одновременном сохранении
- Повреждение файлов состояний
- Некорректное поведение при быстрых действиях пользователя

**Решение:**
1. Добавить мьютекс в `StateStore` для защиты операций записи
2. Добавить проверку `SaveInProgress` в `SaveCurrentState()` и `SaveStateAs()`
3. Блокировать UI во время сохранения

**Приоритет:** ⚠️ Высокий

---

### 5. Неполная валидация при загрузке состояния

**Проблема:** При загрузке состояния валидируется только версия формата, но не все обязательные поля и их структура.

**Местоположение:**
- `ui/wizard/business/state_store.go:171-174` - валидация только версии
- `ui/wizard/presentation/presenter_state.go:164-209` - восстановление без полной валидации

**Последствия:**
- Повреждённые файлы состояний могут привести к панике или некорректному поведению
- Нет проверки обязательных полей (`parser_config`, `config_params`, `selectable_rule_states`, `custom_rules`)
- Нет проверки структуры вложенных объектов

**Решение:**
1. Добавить полную валидацию структуры перед восстановлением
2. Проверить все обязательные поля
3. Валидировать структуру `parser_config` (проверять поля `version`, `proxies`, `outbounds`, `parser` напрямую)

**⚠️ TODO:** Текущая валидация проверяет наличие поля `ParserConfig` внутри `parser_config`. После упрощения структуры валидация должна проверять поля напрямую. Задача не выполнена.
4. Валидировать структуру правил (`rule.raw`, `enabled`, `selected_outbound`)

**Приоритет:** ⚠️ Высокий

---

## 🟡 Средние проблемы

### 6. Неточное отслеживание изменений

**Проблема:** Флаг `hasChanges` устанавливается только при изменении через `SyncGUIToModel()`, но не при прямом изменении модели (например, при программном обновлении правил).

**Местоположение:**
- `ui/wizard/presentation/presenter_sync.go:43-72` - `SyncGUIToModel()` устанавливает флаг
- `ui/wizard/tabs/rules_tab.go` - изменения правил через callback'и

**Последствия:**
- Изменения, сделанные программно, не отслеживаются
- Пользователь может потерять изменения при закрытии визарда

**Решение:**
1. Добавить `MarkAsChanged()` во все места, где изменяется модель напрямую
2. Использовать обёртки для изменения модели, которые автоматически устанавливают флаг

**Приоритет:** 🟡 Средний

---

### 7. Отсутствие обработки частично загруженных состояний

**Проблема:** При ошибке загрузки части состояния (например, невалидное правило) система продолжает работу с неполными данными без предупреждения пользователя.

**Местоположение:**
- `ui/wizard/presentation/presenter_state.go:164-209` - `LoadState()` не обрабатывает частичные ошибки
- `ui/wizard/business/state_store.go:163-183` - `loadStateFromFile()` возвращает ошибку или успех, без частичной загрузки

**Последствия:**
- Пользователь может не заметить, что часть данных не загрузилась
- Некорректное поведение при работе с неполными данными

**Решение:**
1. Добавить валидацию всех полей перед восстановлением
2. Показывать предупреждения о пропущенных невалидных элементах
3. Логировать все ошибки валидации

**Приоритет:** 🟡 Средний

---

### 8. Проблема с типами правил при восстановлении

**Проблема:** При восстановлении состояния тип правила определяется из `rule.raw`, но если правило было изменено пользователем (например, `type` изменён с "System" на "Domains/URLs"), тип может быть определён неправильно.

**Местоположение:**
- `ui/wizard/models/wizard_state_file.go:154-184` - `DetermineRuleType()` определяет тип из `rule.raw`
- `ui/wizard/presentation/presenter_state.go:78-83` - при сохранении используется `ruleType = "System"` для всех предустановленных правил

**Последствия:**
- Если пользователь изменил тип правила на редактируемый, при восстановлении тип может быть определён неправильно
- Правило может потерять возможность редактирования

**Решение:**
1. Сохранять явный `type` для всех правил (не только для пользовательских)
2. При восстановлении использовать сохранённый `type`, а не определять из `rule.raw`

**Приоритет:** 🟡 Средний

---

## 🔵 Мелкие проблемы и улучшения

### 9. Дублирование кода в диалогах

**Проблема:** В `load_state_dialog.go` и `save_state_dialog.go` есть дублирование логики работы с `dialogWindow`.

**Решение:**
- Вынести общую логику в вспомогательные функции

**Приоритет:** 🔵 Низкий

---

### 10. Отсутствие unit-тестов

**Проблема:** Согласно отчёту о реализации, unit-тесты не написаны.

**Решение:**
- Добавить unit-тесты для всех критических функций:
  - `ValidateStateID()`
  - `SaveWizardState()`, `LoadWizardState()`
  - `CreateStateFromModel()`, `LoadState()`
  - Преобразования `RuleState` ↔ `PersistedRuleState`

**Приоритет:** 🔵 Низкий

---

### 11. Нет обработки переполнения директории состояний

**Проблема:** Нет ограничения на количество сохранённых состояний. При большом количестве состояний список может стать неудобным для использования.

**Решение:**
- Добавить ограничение на количество состояний (например, 100)
- Предупреждать пользователя при достижении лимита
- Предложить удалить старые состояния

**Приоритет:** 🔵 Низкий

---

### 12. Нет экспорта/импорта состояний

**Проблема:** Согласно спецификации, экспорт/импорт состояний не реализован (не требуется в первой версии), но это может быть полезно для пользователей.

**Решение:**
- Добавить функции экспорта/импорта состояний в будущих версиях

**Приоритет:** 🔵 Низкий (будущее улучшение)

---

## 📊 Статистика проблем

- 🔴 **Критические:** 2
- ⚠️ **Серьёзные:** 3
- 🟡 **Средние:** 3
- 🔵 **Мелкие:** 4

**Всего:** 12 проблем

---

## 🎯 Рекомендации по приоритетам

### Немедленно исправить (до релиза):

1. ✅ **Кнопка "🔄 Update" не использует `state.json`** - критическая функциональность не работает
2. ✅ **`experimental.clash_api.secret` не сохраняется** - потеря данных при каждом сохранении

### Исправить в ближайшее время:

3. ✅ **Неполное восстановление `config_params`** - потеря пользовательских параметров
4. ✅ **Отсутствие защиты от race conditions** - возможна потеря данных
5. ✅ **Неполная валидация при загрузке** - возможны паники и некорректное поведение

### Улучшить в следующих версиях:

6. ✅ **Неточное отслеживание изменений** - улучшение UX
7. ✅ **Отсутствие обработки частично загруженных состояний** - улучшение надёжности
8. ✅ **Проблема с типами правил** - улучшение функциональности

---

## 🔍 Подводные камни

### 1. Миграция с `config.json` на `state.json`

**Проблема:** Существующие пользователи могут иметь `@ParserConfig` в `config.json`. При первом открытии визарда после обновления нужно:
1. Проверить наличие `@ParserConfig` в `config.json`
2. Если есть - загрузить его и сохранить в `state.json`
3. Удалить `@ParserConfig` из `config.json` (опционально)

**Решение:** Добавить логику миграции при первом открытии визарда.

---

### 2. Конфликт между визардом и кнопкой Update

**Проблема:** Если пользователь работает с визардом и одновременно нажимает "🔄 Update" в главном окне, возможен конфликт:
- Визард использует `state.json`
- Кнопка Update использует `config.json`
- Результаты могут рассинхронизироваться

**Решение:** 
1. Блокировать кнопку Update, если визард открыт
2. Или синхронизировать оба источника данных

---

### 3. Потеря данных при удалении `state.json`

**Проблема:** Если пользователь случайно удалит `state.json`, все настройки визарда будут потеряны. Нет механизма восстановления из бэкапа.

**Решение:**
1. Создавать бэкап `state.json` при каждом сохранении
2. Предлагать восстановление из бэкапа при отсутствии `state.json`

---

### 4. Проблема с путями на разных платформах

**Проблема:** Путь к `bin/wizard_states/` может быть разным на разных платформах. Нужно убедиться, что `FileService.ExecDir()` работает корректно на всех платформах.

**Решение:** Протестировать на всех поддерживаемых платформах.

---

## ✅ Положительные моменты

1. ✅ **Хорошая архитектура:** Чёткое разделение на слои (models, business, presentation, dialogs)
2. ✅ **Соответствие спецификации:** Реализация в целом соответствует спецификации
3. ✅ **Обработка ошибок:** Есть fallback на `config.json` при ошибке загрузки состояния
4. ✅ **Валидация ID:** Правильная валидация ID состояний
5. ✅ **Отслеживание изменений:** Реализован механизм отслеживания несохранённых изменений (с учётом известных проблем)

---

## 📝 Заключение

Реализация системы управления состояниями визарда в целом соответствует спецификации и работает корректно для базовых сценариев. Однако обнаружены **критические проблемы**, которые могут привести к потере данных и некорректной работе приложения:

1. **Кнопка "🔄 Update" не использует `state.json`** - это критическая проблема, которая делает функциональность обновления неработоспособной при использовании визарда.

2. **`experimental.clash_api.secret` не сохраняется** - это приводит к потере данных при каждом сохранении конфига.

**Рекомендация:** Исправить критические проблемы перед релизом. Остальные проблемы можно исправить в следующих версиях, но они также важны для надёжности системы.

---

**Автор:** AI Assistant  
**Дата:** 2026-02-09


