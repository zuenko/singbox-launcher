# Проверка обновлений с попапом при запуске

**Дата создания:** 2026-02-10  
**Статус:** 📋 В работе

---

## 📋 Краткое описание

Реализовать функционал автоматической проверки обновлений при запуске приложения с отображением попапа, если доступна новая версия. Попап должен показывать информацию об обновлении и ссылки на скачивание новой версии.

---

## 🎯 Цели и требования

### Основные требования

1. **Автоматическая проверка при запуске:**
   - При запуске приложения должна выполняться проверка наличия новой версии
   - Проверка выполняется асинхронно, не блокируя запуск приложения
   - Используется существующая функция `CheckLauncherVersionOnStartup()` или расширяется её функциональность

2. **Попап с информацией об обновлении:**
   - Если доступна новая версия, показывается попап (диалог) с информацией
   - Попап содержит:
     - Текущую установленную версию
     - Доступную новую версию
     - Ссылки на скачивание новой версии (GitHub Releases)
   - Попап не блокирует работу приложения (модальный, но не критичный)

3. **Условия показа попапа:**
   - **НЕ показывается**, если приложение запущено с параметром `-tray` (свернуть в трей)
   - **Показывается** при первом открытии основного окна (если не был показан при запуске)
   - Показывается только один раз за сессию (если пользователь закрыл попап, повторно не показывать)

4. **Логика показа:**
   - Если запуск **без** `-tray`: попап показывается после проверки версии (если есть обновление)
   - Если запуск **с** `-tray`: попап не показывается при запуске, но показывается при первом открытии окна через трей

---

## 🔧 Технические детали

### Используемые компоненты

1. **Проверка версии:**
   - `GetLatestLauncherVersion()` - получение последней версии с GitHub
   - `GetCachedLauncherVersion()` - получение закешированной версии
   - `CompareVersions()` - сравнение версий
   - `constants.AppVersion` - текущая версия приложения

2. **UI компоненты:**
   - `fyne.Do()` - для безопасного обновления UI из горутин
   - `dialog.ShowCustom()` или `dialog.ShowInformation()` - для отображения попапа
   - `widget.NewHyperlink()` - для ссылок на скачивание

3. **Параметры запуска:**
   - `-tray` флаг из `main.go` - определяет, нужно ли скрывать окно при запуске

4. **Открытие главного окна:**
   - `ShowMainWindowOrFocusWizard()` - функция для открытия главного окна
   - Вызывается из трей-меню и при клике на иконку в Dock (macOS)

### Структура данных

**Информация об обновлении:**
```go
type UpdateInfo struct {
    CurrentVersion string
    LatestVersion  string
    UpdateAvailable bool
    DownloadURL    string // URL на страницу релизов GitHub
}
```

### URL для скачивания

- **GitHub Releases:** `https://github.com/Leadaxe/singbox-launcher/releases/latest`
- Или прямая ссылка на последний релиз (если доступна через API)

---

## 📐 Архитектура решения

### 1. Расширение CheckLauncherVersionOnStartup

**Текущее поведение:**
- Проверяет версию при старте
- Сохраняет в кеш
- Не показывает UI

**Новое поведение:**
- Проверяет версию при старте
- Сохраняет в кеш
- Если есть обновление И запуск без `-tray`: показывает попап
- Если есть обновление И запуск с `-tray`: сохраняет флаг "показать при открытии окна"

### 2. Новый метод для показа попапа

**Метод:** `ShowUpdatePopupIfAvailable(controller *AppController)`

**Логика:**
1. Получает текущую версию из `constants.AppVersion`
2. Получает последнюю версию из кеша или делает запрос
3. Сравнивает версии через `CompareVersions()`
4. Если есть обновление:
   - Создает диалог с информацией
   - Добавляет ссылки на скачивание
   - Показывает диалог

### 3. Интеграция с открытием окна

**Модификация `ShowMainWindowOrFocusWizard()`:**
- После открытия окна проверяет, нужно ли показать попап обновления
- Показывает попап только если:
  - Есть обновление
  - Попап еще не был показан в этой сессии
  - Запуск был с `-tray` (чтобы показать при первом открытии)

### 4. Управление состоянием

**Новое поле в StateService:**
- `updatePopupShown bool` - флаг, что попап уже был показан в этой сессии
- Методы: `SetUpdatePopupShown()`, `IsUpdatePopupShown()`

**Альтернатива (проще):**
- Использовать локальную переменную в `AppController` для отслеживания показа попапа

---

## 🎨 UI/UX требования

### Дизайн попапа

1. **Заголовок:** "Доступно обновление" / "Update Available"
2. **Содержимое:**
   - Текст: "Доступна новая версия приложения"
   - Текущая версия: `v0.6.0` (пример)
   - Новая версия: `v0.7.0` (пример)
   - Ссылка на скачивание: "Скачать с GitHub" / "Download from GitHub"
3. **Кнопки:**
   - "Закрыть" / "Close" - закрывает попап
   - "Скачать" / "Download" - открывает страницу релизов в браузере (опционально)

### Поведение

- Попап модальный, но не блокирующий (можно закрыть и продолжить работу)
- При клике на ссылку открывается браузер с GitHub Releases
- Попап показывается поверх главного окна (если оно открыто)

---

## 🔄 Сценарии использования

### Сценарий 1: Запуск без `-tray`, есть обновление

1. Пользователь запускает приложение без параметра `-tray`
2. Приложение проверяет версию в фоне
3. Если есть обновление, показывается попап
4. Пользователь видит информацию об обновлении и ссылки

### Сценарий 2: Запуск с `-tray`, есть обновление

1. Пользователь запускает приложение с параметром `-tray`
2. Приложение проверяет версию в фоне
3. Окно скрыто, попап НЕ показывается
4. Пользователь открывает окно через трей
5. При первом открытии показывается попап (если есть обновление)

### Сценарий 3: Запуск без `-tray`, нет обновления

1. Пользователь запускает приложение без параметра `-tray`
2. Приложение проверяет версию в фоне
3. Обновления нет, попап не показывается
4. Приложение работает как обычно

### Сценарий 4: Запуск с `-tray`, нет обновления

1. Пользователь запускает приложение с параметром `-tray`
2. Приложение проверяет версию в фоне
3. Обновления нет, попап не показывается
4. При открытии окна через трей попап не показывается

### Сценарий 5: Повторное открытие окна

1. Пользователь закрыл попап
2. Пользователь закрыл окно
3. Пользователь открывает окно снова
4. Попап НЕ показывается повторно (уже был показан в этой сессии)

---

## 🛠️ Реализация

### Шаг 1: Добавить методы показа попапа

**Файл:** `core/core_version.go`

```go
// ShowUpdatePopupIfAvailable проверяет наличие обновления и показывает попап
// showImmediately: если true, показывает сразу; если false, только если запуск был с -tray
func (ac *AppController) ShowUpdatePopupIfAvailable(showImmediately bool) {
    // Проверка версии и показ попапа
    // Вызывается из CheckLauncherVersionOnStartup при запуске
}

// ShowUpdatePopupIfNeeded проверяет и показывает попап при открытии окна
// Вызывается из OnWindowShown callback
func (ac *AppController) ShowUpdatePopupIfNeeded() {
    // Показывать только если:
    // 1. Запуск был с -tray
    // 2. Попап еще не показывался в этой сессии
    // 3. Есть обновление
}
```

### Шаг 2: Модифицировать CheckLauncherVersionOnStartup

**Файл:** `core/core_version.go`

**Изменить сигнатуру функции:**
```go
// CheckLauncherVersionOnStartup выполняет разовую проверку версии launcher при старте
// startInTray: если true, запуск был с параметром -tray (попап не показывается сразу)
func (ac *AppController) CheckLauncherVersionOnStartup(startInTray bool) {
    // ... существующий код проверки версии ...
    
    // После успешной проверки версии:
    // Если запуск БЕЗ -tray, показываем попап сразу
    // Если запуск С -tray, попап будет показан при первом открытии окна
    if !startInTray {
        ac.ShowUpdatePopupIfAvailable(true)
    }
}
```

### Шаг 3: Интегрировать с открытием окна

**Файл:** `core/services/ui_service.go`

**Добавить callback в UIService:**
```go
type UIService struct {
    // ... существующие поля
    OnWindowShown func() // Вызывается после открытия главного окна
}
```

**Модифицировать `ShowMainWindowOrFocusWizard()`:**
```go
func (ui *UIService) ShowMainWindowOrFocusWizard() {
    // ... существующий код ...
    
    // Вызываем callback после открытия окна (для проверки обновлений)
    if ui.OnWindowShown != nil {
        ui.OnWindowShown()
    }
}
```

**Файл:** `core/controller.go`

**Установить callback в NewAppController:**
```go
// В функции NewAppController, после создания UIService
controller.UIService.OnWindowShown = func() {
    // Проверяем, нужно ли показать попап обновления
    controller.ShowUpdatePopupIfNeeded()
}
```

**Логика `ShowUpdatePopupIfNeeded()`:**
- Проверяет, был ли запуск с `-tray` (хранится в `AppController`)
- Проверяет, был ли попап уже показан в этой сессии
- Если оба условия выполнены, показывает попап

### Шаг 4: Сохранить флаг `-tray` в контроллере

**Файл:** `core/controller.go`

**Добавить поле в AppController:**
```go
type AppController struct {
    // ... существующие поля
    StartInTray bool // Флаг запуска с параметром -tray
    mu          sync.Mutex
}
```

**Добавить метод для установки флага:**
```go
// SetStartInTray сохраняет флаг запуска с параметром -tray
func (ac *AppController) SetStartInTray(startInTray bool) {
    ac.mu.Lock()
    defer ac.mu.Unlock()
    ac.StartInTray = startInTray
}

// WasStartedInTray возвращает, был ли запуск с параметром -tray
func (ac *AppController) WasStartedInTray() bool {
    ac.mu.RLock()
    defer ac.mu.RUnlock()
    return ac.StartInTray
}
```

**Файл:** `main.go`

**Сохранить флаг при запуске:**
```go
// После создания controller
controller.SetStartInTray(*startInTray)

// Передать флаг в CheckLauncherVersionOnStartup
controller.CheckLauncherVersionOnStartup(*startInTray)
```

### Шаг 5: Управление состоянием показа попапа

**Вариант 1:** Локальная переменная в `AppController`
```go
type AppController struct {
    // ... существующие поля
    updatePopupShown bool
    mu               sync.Mutex
}
```

**Вариант 2:** Использовать `StateService`
- Добавить методы в `StateService` для отслеживания показа попапа

---

## ✅ Критерии приемки

1. ✅ При запуске без `-tray` и наличии обновления показывается попап
2. ✅ При запуске с `-tray` попап не показывается при старте
3. ✅ При первом открытии окна после запуска с `-tray` показывается попап (если есть обновление)
4. ✅ Попап содержит текущую и новую версию
5. ✅ Попап содержит ссылку на скачивание новой версии
6. ✅ Попап показывается только один раз за сессию
7. ✅ При отсутствии обновления попап не показывается
8. ✅ Проверка версии не блокирует запуск приложения
9. ✅ Все ошибки обрабатываются корректно (нет обновления = не ошибка)

---

## 🧪 Тестирование

### Тестовые сценарии

1. **Запуск без `-tray`, есть обновление:**
   - Запустить приложение
   - Проверить, что попап появился
   - Проверить содержимое попапа

2. **Запуск с `-tray`, есть обновление:**
   - Запустить с `-tray`
   - Проверить, что попап НЕ появился при старте
   - Открыть окно через трей
   - Проверить, что попап появился

3. **Нет обновления:**
   - Запустить приложение (текущая версия = последняя)
   - Проверить, что попап НЕ появился

4. **Повторное открытие:**
   - Показать попап
   - Закрыть попап
   - Закрыть окно
   - Открыть окно снова
   - Проверить, что попап НЕ появился повторно

5. **Ошибка проверки версии:**
   - Отключить интернет
   - Запустить приложение
   - Проверить, что приложение работает нормально (попап не показывается при ошибке)

---

## 📝 Примечания

### Обработка ошибок

- Если проверка версии не удалась (нет интернета, таймаут), попап НЕ показывается
- Ошибки логируются через `debuglog`, но не показываются пользователю
- Попап показывается только при успешной проверке и наличии обновления

### Производительность

- Проверка версии выполняется асинхронно в горутине
- Не блокирует запуск приложения
- Используется кешированная версия, если доступна

### Совместимость

- Работает на всех поддерживаемых платформах (Windows, Linux, macOS)
- Использует существующие компоненты Fyne для UI
- Не требует новых зависимостей

---

## 🔗 Связанные файлы

- `main.go` - точка входа, параметр `-tray`
- `core/core_version.go` - проверка версий
- `core/services/ui_service.go` - открытие главного окна
- `core/services/state_service.go` - управление состоянием (опционально)
- `internal/constants/constants.go` - текущая версия приложения

---

## 📚 Ссылки

- GitHub Releases: `https://github.com/Leadaxe/singbox-launcher/releases/latest`
- GitHub API: `https://api.github.com/repos/Leadaxe/singbox-launcher/releases/latest`
- Fyne Dialog Documentation: `https://developer.fyne.io/api/v2.4/dialog`

