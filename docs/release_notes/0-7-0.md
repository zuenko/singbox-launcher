
# What's New from 0.6.0

## Version 0.6.2 Release Notes

<details>
<summary><b>🇷🇺 Русская версия / Russian Version</b></summary>

### ✨ Новые функции

#### Поддержка протокола SSH
- **Добавлена поддержка протокола SSH**: Теперь можно использовать SSH-туннели через формат `ssh://`
  - Поддерживается формат URI: `ssh://user:password@server:port#Tag`
  - Поддержка приватных ключей через параметр `private_key_path`
  - Поддержка host keys для проверки подлинности сервера
  - Поддержка passphrase для приватных ключей
  - Настраиваемая версия клиента SSH

#### macOS Installation Script
- **Добавлен скрипт установки для macOS**: Теперь можно установить приложение одной командой
  ```bash
  curl -fsSL https://raw.githubusercontent.com/Leadaxe/singbox-launcher/main/scripts/install-macos.sh | bash -s -- 0.7.0
  ```
- Скрипт автоматически скачивает, распаковывает, устанавливает и запускает приложение
- Автоматически исправляет атрибуты quarantine и права доступа macOS
- Устанавливает приложение в `~/Applications/Singbox-Launcher/`

### 🔧 Улучшения

#### Hysteria2
- **Улучшена поддержка Hysteria2**: Добавлена поддержка короткого формата `hy2://` (официальная спецификация Hysteria 2)
- **Обновлена документация**: Добавлена подробная документация по формату URI для всех протоколов
  - Документированы все поддерживаемые параметры для каждого протокола
  - Добавлены примеры для каждого формата протокола
  - Обновлена документация Hysteria2 согласно официальной спецификации
  - Добавлен параметр `pinSHA256` для Hysteria2
  - Документирована поддержка формата multi-port

### 🐛 Исправления ошибок

#### Парсер подписок
- **Исправлено сохранение всех ProxySource entries**: Теперь все записи с `connections` (без `source`) сохраняются, а не только первая
- **Исправлено сохранение tag_prefix и tag_postfix**: Исправлена ошибка, при которой терялись `tag_prefix` и `tag_postfix` для ProxySource с `connections` при навигации между окнами визарда
- **Исправлено сохранение плейсхолдеров**: Исправлена ошибка, при которой плейсхолдеры вроде `{$tag}`, `{$scheme}` в `tag_prefix`/`tag_postfix`/`tag_mask` повреждались при обновлении конфигурации
- Добавлена логика сопоставления connections для обновления существующих записей вместо замены
- Сохраняются все существующие connections при отсутствии новых connections из ввода

</details>

<details>
<summary><b>🇬🇧 English Version</b></summary>

### ✨ New Features

#### SSH Protocol Support
- **Added SSH protocol support**: Now you can use SSH tunnels via `ssh://` format
  - Supports URI format: `ssh://user:password@server:port#Tag`
  - Support for private keys via `private_key_path` parameter
  - Support for host keys for server authentication
  - Support for passphrase for private keys
  - Configurable SSH client version

#### macOS Installation Script
- **Added installation script for macOS**: Now you can install the app with a single command
  ```bash
  curl -fsSL https://raw.githubusercontent.com/Leadaxe/singbox-launcher/main/scripts/install-macos.sh | bash -s -- 0.7.0
  ```
- Script automatically downloads, extracts, installs, and launches the application
- Automatically fixes macOS quarantine attributes and permissions
- Installs to `~/Applications/Singbox-Launcher/`

### 🔧 Improvements

#### Hysteria2
- **Improved Hysteria2 support**: Added support for short format `hy2://` (official Hysteria 2 specification)
- **Updated documentation**: Added detailed URI format documentation for all protocols
  - Documented all supported parameters for each protocol
  - Added examples for each protocol format
  - Updated Hysteria2 documentation according to official specification
  - Added `pinSHA256` parameter for Hysteria2
  - Documented multi-port format support

### 🐛 Bug Fixes

#### Subscription Parser
- **Fixed preservation of all ProxySource entries**: All entries with `connections` (without `source`) are now preserved, not just the first one
- **Fixed preservation of tag_prefix and tag_postfix**: Fixed bug where `tag_prefix` and `tag_postfix` were lost for ProxySource with `connections` when navigating between wizard windows
- **Fixed placeholder preservation**: Fixed bug where placeholders like `{$tag}`, `{$scheme}` in `tag_prefix`/`tag_postfix`/`tag_mask` were corrupted during configuration update
- Added connection matching logic to update existing entries instead of replacing
- Preserve all existing connections when no new connections from input

</details>

## Version 0.7.0 Release Notes

<details>
<summary><b>🇷🇺 Русская версия / Russian Version</b></summary>

## 🏗️ Архитектурные изменения

### Рефакторинг Wizard (MVP архитектура)
- Полный рефакторинг WizardState в паттерн MVP:
  * `models/` - чистая бизнес-логика данных (WizardModel, RuleState)
  * `presentation/` - слой презентации с разделением GUI состояния
  * `business/` - бизнес-логика без зависимостей от GUI
- Разделение слоя презентации на сфокусированные файлы:
  * `presenter.go` - основная структура презентера
  * `presenter_sync.go` - синхронизация модели/GUI
  * `presenter_async.go` - асинхронные операции
  * `presenter_save.go` - операции сохранения
  * `presenter_methods.go` - методы управления UI
  * `presenter_rules.go` - управление правилами
  * `presenter_ui_updater.go` - реализация интерфейса UIUpdater
  * `gui_state.go` - состояние GUI (только Fyne виджеты)
  * `utils.go` - утилиты Fyne
- Извлечение интерфейсов бизнес-логики:
  * `UIUpdater` - интерфейс для обновлений GUI из бизнес-логики
  * `ConfigService` - адаптер для core.ConfigService
  * `TemplateLoader` - интерфейс для загрузки шаблонов
- Разделение бизнес-функций:
  * `outbound.go` - функции управления outbound
  * `saver.go` - сохранение конфигурации с резервным копированием
  * `config_service.go` - адаптер ConfigService
- Удаление старого WizardState:
  * Удален `ui/wizard/state/state.go` (619 строк)
  * Удален `ui/wizard/state/helpers.go`
- Добавлена полная документация пакетов во всех файлах
- Обновлен ARCHITECTURE.md с новой структурой wizard
- Результат: -553 строки (лучшая организация кода с меньшим количеством кода)

### Улучшения организации кода
- Перемещение SafeFyneDo утилиты из `presenter_ui_updater.go` в `presenter.go` (базовый файл)
- Перемещение констант из `models/constants.go` в `models/wizard_model.go`
- Консолидация документации wizard в единый отчет
- Перемещение документации wizard из `docs/in_progress` в `todo/in_progress`

## ✨ Новые функции

### Поддержка протоколов
- Добавлена поддержка протокола SSH с форматом URI `ssh://` (кастомный формат для singbox-launcher)
- Добавлена поддержка короткой схемы `hy2://` для Hysteria2 (официальная спецификация Hysteria 2)
- Обновлена документация с детальными спецификациями формата URI для всех протоколов:
  * VLESS, VMess, Trojan, Shadowsocks, Hysteria2, SSH
  * Документированы все поддерживаемые параметры для каждого протокола
  * Добавлены примеры для каждого формата протокола
- Улучшена документация Hysteria2 согласно официальной спецификации:
  * Документированы обе схемы `hysteria2://` и `hy2://`
  * Добавлен параметр `pinSHA256`
  * Удалены `upmbps/downmbps` из документации (настройки клиента, не в URI)
  * Документирована поддержка формата multi-port
  * Добавлена ссылка на официальную документацию Hysteria 2 URI Scheme
- Обновлен UI wizard для включения `ssh://` в подсказки и плейсхолдеры
- Добавлены комплексные тесты для парсинга SSH и схемы `hy2://`
- Рефакторинг кода для устранения дублирования в обработке URI Hysteria2

### Улучшения генерации конфигурации
- Реализован трехпроходный алгоритм генерации outbounds с фильтрацией динамических addOutbounds:
  * Pass 1: Создание outboundsInfo и подсчет только узлов (без addOutbounds)
  * Pass 2: Топологическая сортировка зависимостей (алгоритм Кана) и подсчет итогового outboundCount (узлы + валидные addOutbounds)
  * Pass 3: Генерация JSON только для валидных селекторов с фильтрованными addOutbounds
- Добавлена проверка и предупреждения о дублирующихся тегах
- Оптимизация: предварительное выделение памяти для слайсов и мап
- Улучшена обработка циклов в графе зависимостей с детальным логированием
- Логика фильтрации addOutbounds:
  * Динамические селекторы (в outboundsInfo) добавляются только если isValid == true (outboundCount > 0)
  * Константы из template (direct-out, auto-proxy-out и т.д.) всегда добавляются
  * Пустые динамические селекторы не генерируются и не добавляются в addOutbounds
- Исправлено: пустые динамические селекторы больше не добавляются в addOutbounds, что предотвращает ошибки конфигурации sing-box

### Улучшения UI (Servers/Clash API вкладка)
- Исправлена проблема с потерей сортировки при переключении сервера ([#21](https://github.com/Leadaxe/singbox-launcher/issues/21))
- Добавлено сохранение состояния сортировки (тип и направление)
- Сортировка теперь сохраняется и восстанавливается после перезагрузки списка
- Убрана автоматическая сортировка по имени из API (UI управляет сортировкой)
- Добавлено сохранение последнего выбранного прокси ([#21](https://github.com/Leadaxe/singbox-launcher/issues/21))
- При старте sing-box автоматически переключается на последний выбранный прокси
- Упрощена логика: убрана избыточная горутина в AutoLoadProxies

### Улучшения Wizard
- Переименование `CreateVLESSSourceTab` в `CreateSourceTab` (универсальное имя)
- Переименование поля `VLESSURLEntry` в `SourceURLEntry` (поддерживает все протоколы, не только VLESS)
- Обновлены все использования по всему коду (6 файлов)
- Обновлена документация в ARCHITECTURE.md
- Обновлены README и README_RU для использования 'Sources & ParserConfig' вместо 'VLESS Sources'
- Обновлен ParserConfig.md для уточнения, что подписки поддерживают все протоколы
- Вкладка wizard и имена полей теперь корректно отражают, что они поддерживают все протоколы (VLESS, VMess, Trojan, Shadowsocks, Hysteria2, SSH), а не только VLESS
- **Валидация конфига через sing-box check** - интеграция в процесс сохранения:
  * Автоматический запуск `sing-box check` после генерации конфига
  * Кросс-платформная поддержка (Windows скрывает консольное окно)
  * Graceful degradation - пропускает валидацию если sing-box не найден
  * Показывает результат пользователю без блокировки сохранения

### macOS установка
- Добавлен скрипт `scripts/install-macos.sh` для автоматической установки ([#16](https://github.com/Leadaxe/singbox-launcher/issues/16))
- Скрипт сохраняет config.json во время обновления приложения
- Скрипт открывает Finder вместо запуска приложения после установки
- Исправлена двойная префиксация 'v' в URL скрипта
- Обновлены README.md и README_RU.md с двумя методами установки
- Обновлен шаблон релиза в ci.yml с упоминанием скрипта
- Добавлена команда удаления атрибута карантина macOS в шаблон релиза
- Документация подчеркивает, что скрипт установки решает проблемы совместимости с Apple Silicon и macOS Sequoia ([#16](https://github.com/Leadaxe/singbox-launcher/issues/16))

## 🐛 Исправления ошибок

### Критические исправления
- Исправлена совместимость сборки для Intel Mac на Ventura 13.7.8:
  * Добавлены флаги компилятора CGO_CFLAGS и CGO_LDFLAGS с -mmacosx-version-min для явного указания минимальной версии macOS при компиляции
  * Обновлена минимальная версия для Intel-билда с 10.15 до 11.0
  * Это решает проблему неработоспособности приложения на Intel i7 с macOS Ventura 13.7.8

### Исправления парсера и конфигурации
- Исправлено сохранение всех ProxySource записей с connections в ApplyURLToParserConfig:
  * Сохраняются все ProxySource записи без source (с connections) вместо только первой
  * Добавлена логика сопоставления connections для обновления существующих записей вместо замены
  * Сохраняются tag_prefix, tag_postfix, tag_mask, skip и outbounds для сопоставленных connections
  * Сохраняются все существующие connections прокси, когда нет новых connections из входных данных
- Исправлено сохранение tag_prefix и tag_postfix для connections в wizard:
  * Исправлена ошибка, когда tag_prefix и tag_postfix терялись для ProxySource с connections (но без source) при навигации между окнами wizard
  * Добавлена вспомогательная функция restoreTagPrefixAndPostfix для устранения дублирования кода
  * Восстановление tag_prefix и tag_postfix для connections из existingTagPrefixMap
  * Рефакторинг восстановления подписок для использования той же вспомогательной функции
- Исправлено сохранение плейсхолдеров в tag_prefix/tag_postfix/tag_mask во время обновления конфигурации:
  * Исправлена ошибка, когда плейсхолдеры типа `{$num}` и `{$scheme}` в tag_prefix портились до `{}` при обновлении конфигурации
  * Причина: regexp.ReplaceAllString интерпретирует $ как специальный символ для ссылок на группы regex, вызывая замену плейсхолдеров на пустую строку, когда нет соответствующей группы
  * Решение: Использование ручной замены на основе байтов вместо ReplaceAllString для избежания интерпретации $. Это корректно сохраняет все плейсхолдеры: `{$tag}`, `{$scheme}`, `{$protocol}`, `{$server}`, `{$port}`, `{$label}`, `{$comment}`, `{$num}`

### Исправления Clash API
- Добавлена перезагрузка конфигурации Clash API при старте для получения изменений из wizard:
  * Добавлен метод ReloadClashAPIConfig() в APIService
  * Вызов reload перед запуском процесса sing-box
  * Исправлена проблема, когда конфигурация API не обновлялась после исправления сломанной конфигурации в wizard
  * Обеспечивает корректную работу вкладки Servers после обновлений конфигурации
- Улучшены сообщения об ошибках Clash API для Windows:
  * Добавлено сообщение об ошибке, специфичное для Windows, с предложением повторить попытку
  * Показывается 'Please wait 15 seconds and try again' только на Windows
  * Использование проверки runtime.GOOS для определения платформы

### Исправления конфигурации
- Исправлено использование имен процессов macOS в config_template_macos.json
- Обновлен generator.go с исправлениями

### Улучшения парсера подписок
- **Очистка контрольных символов** в VLESS фрагментах/labels: удаление NUL и специальных символов которые могут привести к краху GUI
- Добавлены unit-тесты для валидации очистки контрольных символов

## 📚 Документация

### Обновления документации
- Обновлен ARCHITECTURE.md с новой структурой wizard
- Добавлен план рефакторинга wizard
- Добавлены принятые архитектурные решения в план рефакторинга wizard
- Консолидация всей документации рефакторинга wizard в единый комплексный отчет
- Перемещение финального отчета в todo/complete/wizard_refactoring.md
- Удалены старые фрагментированные файлы документации
- Документированы: формулировка проблемы, детали реализации, результаты, покрытие тестами
- Обновлены README файлы с версией 0.6.2 в примерах установки
- Удалены ссылки на предыдущий релиз из RELEASE_NOTES.md
- Улучшены инструкции по установке в шаблоне релиза ci.yml
- Добавлены подробные пошаговые инструкции для macOS и Windows

### Очистка документации
- Перемещение всех файлов документации wizard из docs/in_progress в todo/in_progress

## 🧪 Тестирование

### Улучшения тестов
- Добавлены unit-тесты для трехпроходного алгоритма генерации outbounds
- Тесты проверяют фильтрацию пустых селекторов
- Тесты проверяют правильность обработки констант
- Тесты проверяют топологическую сортировку цепочки зависимостей
- Добавлены комплексные тесты для парсинга SSH и схемы hy2://
- Добавлены тесты и документация для рефакторинга wizard
- **Интеграционные тесты wizard flow**:
  * TestDefaultWizardFlow_NextNextFinish - эмулирует типичный flow пользователя с реальным URL подписки
  * TestWizardFlowWithCustomRules - проверяет добавление пользовательских правил
  * Валидация JSONC целостности сгенерированного конфига
- **Unit-тесты очистки символов** - валидация удаления контрольных символов из VLESS labels

## 🔄 Прочее

- Объединение веток develop
- Подготовка релиза v0.6.2: добавление release notes и улучшение инструкций по установке
- Обновление шаблона релиза: команда удаления атрибута карантина macOS

### Улучшения CI/CD Pipeline (10 января 2026)
- **Добавлен test_linux.sh** - скрипт тестирования для Linux с фильтрацией UI пакетов (как на macOS/Windows)
- **Исправление ошибок golangci-lint**: разрешены ошибки "imported and not used" для desktop пакета
- **Улучшение выполнения тестов**: добавлено `fail-fast: false` в CI стратегию - теперь платформы тестируются независимо
- **Полный вывод ошибок в CI**: обновлены test_darwin.sh и test_linux.sh для показа реальных ошибок компиляции
- **Удалён поломанный тест**: comments_preservation_test.go (сравнивал несвязанные файлы)
- **Стабилизация CI**: добавлен `nopause` параметр для предотвращения интерактивных промптов в неинтерактивной среде


## 🏗️ Architectural Changes

### Wizard Refactoring (MVP Architecture)
- Complete refactoring of WizardState into MVP pattern:
  * `models/` - pure business data (WizardModel, RuleState)
  * `presentation/` - presenter layer with GUI state separation
  * `business/` - business logic without GUI dependencies
- Split presentation layer into focused files:
  * `presenter.go` - core presenter structure
  * `presenter_sync.go` - model/GUI synchronization
  * `presenter_async.go` - async operations
  * `presenter_save.go` - save operations
  * `presenter_methods.go` - UI management methods
  * `presenter_rules.go` - rules management
  * `presenter_ui_updater.go` - UIUpdater interface implementation
  * `gui_state.go` - GUI state (Fyne widgets only)
  * `utils.go` - Fyne utilities
- Extract business logic interfaces:
  * `UIUpdater` - interface for GUI updates from business logic
  * `ConfigService` - adapter for core.ConfigService
  * `TemplateLoader` - interface for template loading
- Separate business functions:
  * `outbound.go` - outbound management functions
  * `saver.go` - config saving with backup
  * `config_service.go` - ConfigService adapter
- Remove old WizardState:
  * Deleted `ui/wizard/state/state.go` (619 lines)
  * Deleted `ui/wizard/state/helpers.go`
- Added comprehensive package documentation to all files
- Updated ARCHITECTURE.md with new wizard structure
- Net result: -553 lines (better code organization with less code)

### Code Organization Improvements
- Moved SafeFyneDo utility from `presenter_ui_updater.go` to `presenter.go` (base file)
- Moved constants from `models/constants.go` to `models/wizard_model.go`
- Consolidated wizard documentation into single report
- Moved wizard documentation from `docs/in_progress` to `todo/in_progress`

## ✨ New Features

### Protocol Support
- Added SSH protocol support with `ssh://` URI format (custom format for singbox-launcher)
- Added support for `hy2://` short scheme for Hysteria2 (official Hysteria 2 specification)
- Updated documentation with detailed URI format specifications for all protocols:
  * VLESS, VMess, Trojan, Shadowsocks, Hysteria2, SSH
  * Documented all supported parameters for each protocol
  * Added examples for each protocol format
- Improved Hysteria2 documentation according to official specification:
  * Documented both `hysteria2://` and `hy2://` schemes
  * Added `pinSHA256` parameter
  * Removed `upmbps/downmbps` from docs (client-side settings, not in URI)
  * Documented multi-port format support
  * Added reference to official Hysteria 2 URI Scheme documentation
- Updated wizard UI to include `ssh://` in hints and placeholders
- Added comprehensive tests for SSH parsing and `hy2://` scheme
- Refactored code to eliminate duplication in Hysteria2 URI handling

### Configuration Generation Improvements
- Implemented three-pass algorithm for outbound generation with filtered dynamic addOutbounds:
  * Pass 1: Create outboundsInfo and count only nodes (without addOutbounds)
  * Pass 2: Topological sort of dependencies (Kahn's algorithm) and count final outboundCount (nodes + valid addOutbounds)
  * Pass 3: Generate JSON only for valid selectors with filtered addOutbounds
- Added duplicate tag checking and warnings
- Optimization: pre-allocate memory for slices and maps
- Improved cycle handling in dependency graph with detailed logging
- addOutbounds filtering logic:
  * Dynamic selectors (in outboundsInfo) are added only if isValid == true (outboundCount > 0)
  * Constants from template (direct-out, auto-proxy-out, etc.) are always added
  * Empty dynamic selectors are not generated and not added to addOutbounds
- Fixed: empty dynamic selectors no longer added to addOutbounds, preventing sing-box configuration errors

### UI Improvements (Servers/Clash API Tab)
- Fixed issue with losing sort order when switching servers ([#21](https://github.com/Leadaxe/singbox-launcher/issues/21))
- Added sort state preservation (type and direction)
- Sort order now persists and restores after list reload
- Removed automatic name sorting from API (UI manages sorting)
- Added last selected proxy preservation ([#21](https://github.com/Leadaxe/singbox-launcher/issues/21))
- On sing-box start, automatically switches to last selected proxy
- Simplified logic: removed redundant goroutine in AutoLoadProxies

### Wizard Improvements
- Renamed `CreateVLESSSourceTab` to `CreateSourceTab` (universal name)
- Renamed `VLESSURLEntry` field to `SourceURLEntry` (supports all protocols, not just VLESS)
- Updated all usages across codebase (6 files)
- Updated documentation in ARCHITECTURE.md
- Updated README and README_RU to use 'Sources & ParserConfig' instead of 'VLESS Sources'
- Updated ParserConfig.md to clarify subscriptions support all protocols
- Wizard tab and field names now correctly reflect that they support all protocols (VLESS, VMess, Trojan, Shadowsocks, Hysteria2, SSH), not just VLESS
- **Config validation via sing-box check** - integrated into save process:
  * Automatic `sing-box check` run after config generation
  * Cross-platform support (Windows hides console window)
  * Graceful degradation - skips validation if sing-box not found
  * Shows result to user without blocking save

### macOS Installation
- Added `scripts/install-macos.sh` script for automated installation ([#16](https://github.com/Leadaxe/singbox-launcher/issues/16))
- Script preserves config.json during app update
- Script opens Finder instead of launching app after installation
- Fixed double 'v' prefix in script URL
- Updated README.md and README_RU.md with two installation methods
- Updated release template in ci.yml with script mention
- Added macOS quarantine attribute removal command to release template
- Documentation emphasizes that installation script resolves compatibility issues with Apple Silicon and macOS Sequoia ([#16](https://github.com/Leadaxe/singbox-launcher/issues/16))

## 🐛 Bug Fixes

### Critical Fixes
- Fixed build compatibility for Intel Mac on Ventura 13.7.8:
  * Added CGO_CFLAGS and CGO_LDFLAGS compiler flags with -mmacosx-version-min to explicitly specify minimum macOS version during compilation
  * Updated minimum version for Intel build from 10.15 to 11.0
  * This resolves application inoperability issue on Intel i7 with macOS Ventura 13.7.8

### Parser and Configuration Fixes
- Fixed preservation of all ProxySource entries with connections in ApplyURLToParserConfig:
  * Preserve all ProxySource entries without source (with connections) instead of only first one
  * Added connection matching logic to update existing entries instead of replacing
  * Preserve tag_prefix, tag_postfix, tag_mask, skip, and outbounds for matched connections
  * Keep all existing connections proxies when no new connections from input
- Fixed preservation of tag_prefix and tag_postfix for connections in wizard:
  * Fixed bug where tag_prefix and tag_postfix were lost for ProxySource with connections (but no source) when navigating between wizard windows
  * Added helper function restoreTagPrefixAndPostfix to eliminate code duplication
  * Restore tag_prefix and tag_postfix for connections from existingTagPrefixMap
  * Refactored subscription restoration to use the same helper function
- Fixed preservation of placeholders in tag_prefix/tag_postfix/tag_mask during config update:
  * Fixed bug where placeholders like `{$num}` and `{$scheme}` in tag_prefix were being corrupted to `{}` when updating configuration
  * Root cause: regexp.ReplaceAllString interprets $ as special character for regex group references, causing placeholders to be replaced with empty string when no matching group exists
  * Solution: Use manual byte-based replacement instead of ReplaceAllString to avoid $ interpretation. This preserves all placeholders correctly: `{$tag}`, `{$scheme}`, `{$protocol}`, `{$server}`, `{$port}`, `{$label}`, `{$comment}`, `{$num}`

### Clash API Fixes
- Added Clash API config reload on Start to pick up wizard changes:
  * Added ReloadClashAPIConfig() method to APIService
  * Call reload before starting sing-box process
  * Fixes issue where API config wasn't updated after wizard fixes broken config
  * Ensures Servers tab works correctly after config updates
- Improved Clash API error messages for Windows:
  * Added Windows-specific error message with retry suggestion
  * Show 'Please wait 15 seconds and try again' only on Windows
  * Use runtime.GOOS check for platform detection

### Configuration Fixes
- Fixed use of macOS process names in config_template_macos.json
- Updated generator.go with fixes

### Subscription Parser Improvements
- **Control character sanitization** in VLESS fragments/labels: removes NUL and special characters that could crash GUI
- Added unit tests for control character sanitization validation

## 📚 Documentation

### Documentation Updates
- Updated ARCHITECTURE.md with new wizard structure
- Added wizard refactoring plan
- Added accepted architectural decisions to wizard refactoring plan
- Consolidated all wizard refactoring documentation into single comprehensive report
- Moved final report to todo/complete/wizard_refactoring.md
- Removed old fragmented documentation files
- Documented: problem statement, implementation details, results, test coverage
- Updated README files with version 0.6.2 in installation examples
- Removed references to previous release from RELEASE_NOTES.md
- Improved installation instructions in ci.yml release template
- Added detailed step-by-step instructions for macOS and Windows

### Documentation Cleanup
- Moved all wizard documentation files from docs/in_progress to todo/in_progress

## 🧪 Testing

### Test Improvements
- Added unit tests for three-pass outbound generation algorithm
- Tests verify empty selector filtering
- Tests verify correct constant handling
- Tests verify topological sort of dependency chain
- Added comprehensive tests for SSH parsing and hy2:// scheme
- Added tests and documentation for wizard refactoring
- **Wizard integration tests**:
  * TestDefaultWizardFlow_NextNextFinish - emulates typical user flow with real subscription URL
  * TestWizardFlowWithCustomRules - verifies custom rule addition
  * JSONC integrity validation of generated config
- **Control character sanitization unit tests** - validates removal of control chars from VLESS labels

## 🔄 Miscellaneous

- Merged develop branches
- Prepared release v0.6.2: added release notes and improved installation instructions
- Updated release template: macOS quarantine attribute removal command

### CI/CD Pipeline Improvements (January 10, 2026)
- **Fixed golangci-lint errors** (commit 37f109d):
  * Resolved "imported and not used" errors for desktop package (added //nolint:unused directive)
  * Removed unused jsonc import from test files
  * Removed undefined function calls in test code
  
- **Improved test execution and error reporting** (commit de0e6f4):
  * Added `fail-fast: false` to CI strategy matrix to ensure all platforms (Ubuntu, macOS, Windows) run independently
  * Previously: fail-fast: true caused subsequent platform tests to be skipped when one platform failed
  * Updated test_darwin.sh to show full error output (changed `2>/dev/null` to `2>&1`)
  * Now CI shows actual compilation errors instead of generic "Failed to compile" messages
  
- **Removed broken test and cleanup** (commit d035ca6):
  * Deleted `ui/wizard/business/comments_preservation_test.go` - broken test that compared unrelated files
  * This test tried to verify "comment preservation" but compared config.json with config_template.json
  * Not applicable to current architecture where configs are generated fresh, not edited in-place
  * Fixed code indentation in main.go (log file close operations)
  
- **Stabilized CI for all platforms** (commit 6b12215):
  * Added `nopause` parameter to test script invocations for Ubuntu and macOS
  * Guards interactive pause in test_darwin.sh with TTY check (`[ -t 0 ]`)
  * Prevents `set -e` from failing on `read` command in non-interactive CI environments
  * All platform tests now pass reliably without exit code 1 from interactive prompts

### Test Coverage
- Verified `wizard_integration_test.go` passes successfully:
  * Tests default wizard flow (next-next-finish scenario)
  * Tests wizard flow with custom rules
  * Validates JSONC output integrity
  * All tests pass with proper error handling

