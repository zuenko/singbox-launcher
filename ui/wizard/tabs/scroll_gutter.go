package tabs

// scrollbarGutterWidth is reserved space to the right of scrollable content so the
// native scrollbar strip does not overlap buttons or text (shared by Sources, Rules, DNS, library dialog).
const scrollbarGutterWidth = 10
